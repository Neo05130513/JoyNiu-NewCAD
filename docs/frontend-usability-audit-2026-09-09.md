# 前端客户使用验收 · 2026-09-09

分支：`V2-detail`。本轮使用本地前端 `localhost:5173`，按客户操作测试，跳过上传原图生成 3D 及 AI 建模请求。浏览器验收使用单独标签页和新建验收项目，不修改客户现有模型参数。

## 已修复和补充

| 问题 | 结果 |
| --- | --- |
| 文件缺少复制、项目与文件缺少可恢复删除 | 添加独立副本及回收站恢复 |
| 能导出备份却不能导入 | 项目页、设置页均可导入；兼容旧备份及单文件 JSON，追加恢复 |
| 跨项目返回没有保持最后打开文件 | 保存并恢复各项目最后打开文件 |
| 版本快照显示内部参数名 | 使用中文参数及实际派生值 |
| 错误尺寸引导客户重新上传图纸 | 显示“修正参数”，定位到错误输入 |
| 手动草稿显示 AI 分析已完成 | 使用创建草稿、设置尺寸流程 |
| 重复点同一视角不复位；缩放百分比不符 | 同视角可复位，真实缩放与按钮范围统一 |
| 预览或投影失败缺少恢复操作 | 原 GLB 重载；投影失败重试、缩放和下载 |
| 全隐藏图层仍能导出空工程图 | 提示恢复图层，并阻止空 DXF |
| 新通用模型未参与装配主件检查 | 使用有效实体包络，保持真实坐标和失效状态 |
| 旋转件、螺钉头的放置间距不合理 | 按完整旋转包络计算复制与插入间距 |
| 快捷命令和弹窗关闭交互不完整 | 方向键、回车、Esc；内侧空白不误关；导出菜单外点关闭 |
| PDM 只显示前五项，剩余要求客户访问 API | 全部文档搜索、分页、展开历史版本和下载 |
| 退出账号等情况下旧请求回写、忙碌不结束 | 按账号、文件与模型校验上下文，失效请求不得污染新状态 |
| 只读成员因缺少 CAM 权限无法查看 PDM | 分离文档清单与 CAM 恢复，保持授权文档可见 |

## 实际浏览器操作

以下通过真实页面控件完成：

- 创建验收项目和基础轴；将轴长设为 86 mm；孔径大于外径时显示中文错误并暂停预览，改回后恢复。
- 保存 v01；复制设计并将副本长度设为 92 mm；删除副本、从回收站恢复、刷新，副本仍为 92 mm，原件仍为 86 mm。
- 新建文档、编辑正文并保存版本；另写新正文后恢复历史版本，恢复前的修改自动留下 v02；刷新后正文仍正确。
- 下载工程图 DXF；切换主视图及 2:1 显示比例，画面尺寸文字仍为实际 86 mm。
- 导出项目备份；无效 JSON 显示错误且保留当前项目；有效验收备份新增同名的“导入”项目，保留三份文件及三条历史版本。
- 使用快捷命令搜索“项目管理”，回车正确进入；导出菜单打开后 Esc 正确关闭。
- 标准件搜索 `M8x30`，插入螺钉；旋转 90°、复制实例、运行预检查、移除并撤销，实例数和坐标同步。
- 装配爆炸视图、剖切及对应滑条出现；三维拖动后再次点击前视恢复正视图。
- PDM / CAM 未登录状态禁用对应操作，并显示登录要求；角色中文、设置和帮助入口可达。
- 390 × 844 视口检查项目管理、导航与建模页，页面宽度为 390 px，无横向溢出；画布约 364 × 308 px，可操作。检查后还原视口。

## 隔离交互场景

为避免修改真实账号、项目共享或制造状态，使用临时页面渲染实际组件与合成数据：

- 13 项 PDM 文档分页到第二页，查看第 13 项；按历史文件名搜索、展开版本及下载。
- 投影显示 150% 并下载；模拟不存在的投影文件，显示重试入口；恢复文件后点击重试，原图成功载入（800 px）。

临时页面、脚本和 SVG 已移除。真实注册、共享成员、CAM 审核及 NC 放行未执行；这些前端权限及异步边界以代码审查和回归用例验证。本轮没有重新验证 AI 识图准确性或生成实体质量。

## 自动验证

- `node --test src/*.test.js`：216 项通过，0 失败。
- `npm run build`：通过。仍有原有单包体积提示，不影响构建。
- `git diff --check`：通过。

备份不嵌入原图或实体二进制；跨机器恢复后，原服务器需可访问才能读取这些文件。装配仍是简化标准件与包络预检查，通用实体投影不是带完整制造标注的 DXF。

## 我的项目改版追加验收

取消项目下拉选择，改为常驻项目导航与文件卡片，默认跨项目展示全部文件。常用打开操作直接可见，其他文件操作收入更多菜单。

实际页面验收：

- 从项目页创建项目后仍留在项目管理；创建文档、自动保存正文和保存版本成功。
- 当前位于 B 项目时，搜索并重命名 A 项目文档、保存 A 的版本；B 的当前文件保持不变，版本快照为 A 的完整正文。
- 跨项目复制后选中目标项目副本，正文独立修改；在 B 项目删除 A 副本，回收站恢复后副本正文和原件正文分别保持。
- 搜索结果点击文档直接进入所属项目编辑器；点击项目导航只展示文件，不自动跳到文档编辑器。
- 同项目搜索中复制并指定不包含原关键词的新名称，搜索自动跟随新名称，副本仍可见。
- 文件类型空结果与清除筛选正常；更多菜单 Esc 关闭；旧模型卡片直接打开 3D 工作台成功，浏览器控制台无错误。
- 临时内存页面使用 12 项目、22 文件、1 个空项目及 120 字长项目/文件名。定位并修复长名称撑宽列表行的问题，修复后第 12 项目与其目标文件一次点击正确打开。空项目显示新建文件入口。
- 390 × 844 视口下，项目列表使用两列并可滚动，包含 120 字名称时文档宽度仍为 390 px，工作区没有横向溢出。桌面截图检查卡片与列表布局。

自动检查：216 项现有测试通过；17 个基于真实 App 回调和 ProjectStore 的独立场景验证跨项目目标、默认兼容和切换拒绝行为。构建与 git diff --check 通过。临时视觉页面和脚本删除，创建的 A/B 验收项目移入回收站，原有项目保留。
