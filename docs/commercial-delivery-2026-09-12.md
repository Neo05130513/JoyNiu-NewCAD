# 线下积分与客户交接实施记录 · 2026-09-12

本轮按用户确认补齐线下收款运营、Token 积分扣费、客户说明及运维。AI 服务接入和正式访问／账号安全暂缓，不更换模型、Codex 登录、出口或管理员凭据。线上购买继续独立关闭。

## 计费口径

充值 10 元 = 1000 积分；服务器由实际收款分值计算积分。消费以 OpenAI Standard API 美元价为折算基准，乘后台保存的人民币结算汇率、100 积分/元及收费倍率。初始倍率 1，汇率 7 是可调的固定结算参数，不是实时银行报价。

2026-09-12 核对 [官方价格](https://developers.openai.com/api/docs/pricing) 和 [GPT-6 Astra 模型页](https://developers.openai.com/api/docs/models/gpt-6-astra)：每百万 Token 普通输入 $10、缓存读取 $1、缓存写入 $12.5、输出 $50。单请求输入超过 272,000 时，全请求输入/缓存价 ×2、输出价 ×1.5。缓存读取与写入从总输入中分离；推理已经包含在输出中。

- 新规则版本锁定价格目录、汇率、倍率；只能对之后启动的任务生效，不追溯旧账。
- `ready`、`review_required` 成功候选轮次收费；`failed`、`needs_input`、`cancelled`、`interrupted` 不收费。已有原图的补定位属于查看核对，免收费用。
- 全部调用用精确分数计算，一轮合计后仅向上取整一次到整数积分。举例：100,000 输入（20,000 缓存读取、30,000 缓存写入）、10,000 输出，官方等价 $1.395；汇率7、倍率1是976.5分，本轮扣977分。倍率2是1953分。
- Codex 的聚合输入不能当作一个长请求。未确认的长上下文、缓存写入附加费用免收并标注；核心用量缺失保留待核查。API等价折算金额不伪装为订阅的实际现金成本。
- 余额不足时扣完余额，差额记待补缴；充值优先补缴，补足后才能启动新收费任务。
- 没有费用预估／最高消费提示、积分冻结或预算到顶暂停。

## 已补齐的运营功能

- 后台登记线下充值：搜索客户、填写实收金额及凭证编号、人工确认到账。凭证跨操作人唯一，重放不会重复加分。新客可先到账，充值不代表替客户接受服务条款。
- 充值、赠送、补偿、调账分开；订单/流水可按客户、来源、凭证和日期检索，收入只统计实际收款。
- 全额线下退款关联原单、退款凭证和可回收余额，保留原记录；可联动原退款申请。仅记录已发生的线下退款，不转账。部分退款、已消费款和已开发票红冲仍需人工核对。
- 客户充值页、公开计费说明与帮助同步线下流程；账单可查看规则版本、倍率、汇率及费用明细。
- 数据删除申请明确范围、客户确认和本人任务关联；客服登记实际处理范围、保留依据/期限、备份安排和证据编号。申请或回执本身不执行物理删除。
- 客户使用指南、支持范围、交付验收表、数据处理操作说明和三类条款草稿齐备。
- 宿主服务/磁盘/队列/上游失败/备份监测及后台安全读取，报告超过15分钟标为过期。协调备份先关准入、核实无活跃任务，再停写、复制、校验及恢复服务。

## 验收证据

- 精确计价、长上下文边界、未知附加费用免收、失败不收费、倍率快照、幂等重放、待补缴和充值补欠费的隔离后端测试通过。
- 本地浏览器保存倍率1.25/汇率7.1234的新版本；服务端普通输入费率8904.25分/百万，无真实收费启用；390px布局无整页横向溢出。
- 独立本地QA客户：10元充值1000分，同凭证再次提交拒绝，移动端登记全额退款，余额、账本净额及净收入回到0；浏览器无脚本错误。记录 `/tmp/joyniu-commercial-staging/offline-qa-result.json`。
- 30张既有原图哈希未变化；8份既有STEP导入→再导出→回读通过实体有效性、尺寸和体积检查。7份任务仍待确认、1份待补充；不是客户CAD验收。报告见 [离线STEP回读证据](evidence/step-roundtrip-2026-09-12.json)。
- 发布前后端504项、前端414项通过，生产构建通过。新镜像在禁网、独立数据库下通过真实HTTP启动检查。
- 运维专项132项、前端对应13项通过，覆盖失败恢复、写入并发、备份篡改/保留保护和陈旧监控；其中42项恢复专项包含真实HTTP路径和跨进程文件锁。上述专项与总回归有重叠，不叠加为总数。没有使用故障注入干扰生产客户。

## 仍需外部完成

1. 正式条款需要真实经营主体、售后联系方式、保存期限等资料。草稿保留占位时不发布，不代客户接受；实际扣分须在条款准备好后启用。
2. 已完成服务器到用户Mac的一次性加密副本及解密校验；持续异地备份的固定目标与站外告警接收方式仍未配置，系统分别标明。该副本不包含Docker镜像归档，异机环境重建和持续灾备仍需验收。
3. 客户代表性图纸的补答、修改、人工尺寸验收及其实际NX/SolidWorks/Creo打开测试，不能由已有模型的OCCT回读替代。
4. 物理删除由运营按范围核查、执行并登记回执；本轮没有自动批量清理客户原图或历史记录。

## 已发布及生产验证

2026-09-12 发布 `offline-credits-20260912` 至 [客户网站](http://122.51.168.205/) 和 [管理后台](http://122.51.168.205/admin)。本轮未生成Git提交；以经测试的工作区发布包及镜像ID标识此版，保留 `admin-login-20260911` 镜像供代码回滚。

| 服务 | 镜像 | 完整镜像ID |
| --- | --- | --- |
| API | `joyniu-cad-api-codex:offline-credits-20260912` | `sha256:bed56d48945238eb7bdec6d1ca8ca75a023bf7e658a37ebf89ac75f5dfb03c41` |
| Web | `joyniu-cad-web:offline-credits-20260912` | `sha256:bfbdd4088a9dcc83be7b09ffe6f2bcbfcd32604b6a3345c38113e0a25ec0647e` |

- 停写发布备份 `/opt/joyniu-backups/pre-offline-credits-20260912` 校验1010文件、2库。未清空客户数据或更换模型、登录和出口。
- 生产初始规则 `bp_c9024dba39134af59bd0c6491341dd44` 已保存并选用，倍率1、固定结算汇率7。`chargingEnabled=false`、`onlinePaymentEnabled=false`、`offlineRechargeEnabled=true`、`termsReady=false`。仅配置规则，未对真实客户充值、退款、扣分或执行模型任务。
- 公网14项检查通过；真实浏览器完成管理员登录、计费规则查看、线下充值表单打开/取消、系统状态与390px布局检查，无脚本错误，退出后会话返回401。模型仍为 `codex / gpt-6-astra / high`。
- 已安装宿主监控（每5分钟）和空闲时备份（每15分钟检查、距上次有效备份至少24小时）；报告进入后台。没有删除旧备份或启用自动清理。
- 首次备份成功复制后，恢复探针错误访问不存在的 `auth/capabilities`，导致健康服务被误报恢复超时。已修正为真实 `auth/account-capabilities`，补充HTTP与锁占用回归，重新校验同次快照后恢复报告为completed；原故障信息仍保留。
- 定时备份 `/opt/joyniu-backups/scheduled/scheduled-20260912T110747Z-afb190f6` 校验1039文件、2库，已实际恢复至全新目录 `/opt/joyniu-restores/offline-credits-20260912-check` 并校验。生产数据未被覆盖，准入维护标志已撤销，API healthy，定时器已启用。
- 修正脚本后再做一次完整协调备份验收，`scheduled-20260912T111611Z-708d62ea`（1039文件、2库）直接返回completed及recovery complete，包含已修复源码。仅单次验收配置取消间隔限制，正式24小时备份间隔不变；与定时器撞锁时安全跳过，未并行停写。
- 新快照实际恢复至 `/opt/joyniu-restores/offline-credits-20260912-final` 再次通过；随后经SSH核验传输至用户自己的Mac，AES-256-GCM密文60,825,656字节。密文在 `/Users/neo/Library/Application Support/JoyNiu-CAD/Backups/offline-credits-20260912.enc`，随机密钥单独存于同级 `backup-keys/offline-credits-20260912.key`（两个目录0700、文件0600，未记录密钥内容）。实际解密校验1039文件/2库通过，临时明文已清理。工具及原备份36项测试通过。此操作没有启用持续异地同步或站外通知。
