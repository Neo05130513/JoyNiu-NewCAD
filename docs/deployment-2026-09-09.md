# 2026-09-09 服务器部署记录

- 地址：<http://122.51.168.205/>，目录 `/opt/joyniu-cad`。
- 来源：本地 `V2-detail` 工作区，包含本次尚未提交的前后端改动。
- 旧版 JoyNiu、RAGFlow 的容器、数据卷、应用目录和旧应用镜像已清理；保留操作系统、SSH 和 Docker。
- 清理前备份已下载到本机 `~/Desktop/服务器备份/122.51.168.205/2026-09-09/旧应用配置与数据.tar.gz`，权限 600。两端 SHA-256 一致：`31416dbf05fcbf5de2e532fab21e2ba8636b3a913cc72c2fbc0712dd2670dfbb`。
- 初始管理员账号与密码在同目录的 `新项目登录信息.md`，权限 600；先完成内部初始化，之后才开放网站。

## 运行配置

Nginx 对外监听 80；API 仅在 Docker 网络监听 8010，以 UID 10001 运行。两个容器均配置自动重启，Docker 已设置开机启动。宿主机对外监听的 TCP 端口为 22 和 80。

SQLite 与 CAD 原图/修订存放在宿主机 `data/`。密钥保存在 `deploy/.env.production`（600），未写入镜像和代码仓库。服务器通过项目已有的模型 API 提供建模服务，配置模型为 `gpt-5.6-sol`，需要用户登录。模型服务连通性和凭证检查通过；未迁移本机 Codex 登录。

当前使用 IP + HTTP，未配置域名和 HTTPS。前端项目列表属于当前浏览器本地存储，电脑上的项目不会因部署自动迁移；可使用项目备份导入功能。

## 验证结果

- 前端 224 项测试通过，使用 `VITE_API_BASE=/api/v1` 构建；公网 HTTP 所需的文件编号与同源下载地址兼容修复已包含。
- 公网页面、实际 JS/CSS、健康检查、AI 配置、CAD 能力接口均返回预期结果。
- 匿名账号/项目接口返回 401；管理员登录、身份及项目列表返回 200。
- 容器内隔离 CAD 子进程以 UID 10001 生成 20 × 15 × 10 mm 实体，STEP、GLB、三视图和 15 秒预算的草稿检查全部通过。
- 公网非 AI 支架生成及 STEP/GLB 下载通过，文件大小与 SHA-256 匹配。测试实体只在内存，随后重建 API 容器清理。
- API 容器重建后再次通过登录与接口检查，管理员账号及配置保持有效。
- 浏览器检查首页、项目页和账号入口，控制台未发现错误。
- 几何 CadQuery 2.8.0、DWG LibreDWG 0.13.3、PDF PyMuPDF、OCR Tesseract 5.5.0 均可用。此次未重复测试上传图纸后的 AI 建模流程。

维护与再次验收见 [部署说明](../deploy/README.md)。

## 登录页面修复

部署后的页面仍预填开发账号密码，用户直接点击登录时与服务器初始化的管理员密码不匹配。修复后登录表单不预填任何凭证，并要求填写邮箱和密码；正常登录不再显示首次注册名称和角色选项。首次初始化入口根据只读账号状态显示，生产环境通过部署流程初始化管理员。

管理员密码保持不变，仍使用本机登录信息文件中的初始密码。前端 224 项测试、后端 11 项鉴权/状态/OpenAPI 测试通过；部署验收增加账号已初始化检查。

修复已发布到服务器，使用原登录信息再次通过管理员登录及身份查询。浏览器确认邮箱、密码均为空且必填，空表单会提示填写字段，已初始化站点不再出现注册按钮。


## 图纸 23 建模失败与草稿预览修复

服务器记录显示，实体已通过 OCCT 几何检查并生成 GLB/STEP，但独立图纸复核先后出现总预算耗尽、结果 JSON 格式异常，以及单次 180 秒超时。旧前端仅接收成功状态的实体，导致这些已保存的草稿显示为空白。

复核器改用严格 JSON schema，独立复核默认 medium，在同一预算内对格式/流异常最多补试一次，并保留逐视图证据校验及交付门槛。前端仅对当前修订、有效实体、明确复核服务异常且无已知图纸差异的结果恢复草稿预览；不恢复过期或已修改的模型，不提供 STEP 交付。旧项目中 generation 为空的符合条件记录可在重新打开时恢复。

验证：前端 230 项测试、后端复核及交付相关 188 项测试通过，建模流程独立复核回归通过。用服务器保存的原图和实际投影单独验证修复版，一次请求约 53 秒返回合法 mismatch，正确发现斜臂、带孔耳部等缺失。此次诊断没有将该结果写入用户任务，也没有把未完成模型改为已通过；完整几何仍需建模流程继续修正。

修复已部署，API/Web 容器健康，管理员登录及公网资源检查通过。两份服务器真实失败响应均通过草稿适配与旧快照恢复验证；浏览器通过备份导入真实失败记录后成功载入 GLB，显示“实体草稿 · 图纸复核未完成”和“继续图纸复核”，控制台无错误。验证副本仅创建在独立浏览器中，随后移入回收站，未修改用户 Chrome 项目或服务器运行记录。


## 图纸 9 规划阶段超时排查

9.jpg 的新任务与前一次独立复核失败不同：原图转录成功，6 项独立尺寸证据已保存，但尚未保存首份 CAD 计划。整轮 900 秒中，最后一个 high 规划动作耗用剩余约 471 秒，约 407 秒才开始返回正文，随后在 JSON 尚未完成时被总期限截断。没有收到完成的计划，不执行部分 JSON。页面的“已尝试 8 次”实际上包括读图、辅助空间分析和正常规划轮次，本轮没有自动重试。

本次修复将规划请求分段限时，首份计划之前使用独立的 medium 推理设置，并明确要求先保存 1–3 个特征的最小完整草稿。没有完成动作的长超时即使出现部分文字，续接仍适用有界恢复；实际几何与原图复核门槛不变。前端分别显示模型调用次数和真实重试次数，并按实际阶段与已保存内容说明失败。

首批修复通过后端406项、前端236项检查，已部署并通过服务及管理员登录验收。使用用户当前Chrome会话通过正常“重试建模”入口续接9.jpg，真实首份草稿以medium在约78秒内保存，并通过OCCT草稿构造检查；过程中一次上游503已自动重试。后续又暴露连续inspect_source耗时与辅助空间分析失败清空ledger的续接缺口：任务在约740秒结束，单次180秒上限已生效，保留1个草稿特征与6项观测，没有生成完整模型或放行。

追加修复保留同源、同转录的合法观测；辅助分析failed/running/not_completed本身不清空进度，只有完整候选中的关键坐标基准变化才重新检查。对重复包裹的measurement expected，只在kind与重复axis/probe一致且无未知字段时去掉一层，随后仍使用原数值验证。连续查看原图而不构造的操作也受限制，要求利用保留的图纸细节保存草稿或提出具体问题。

追加修复已发布，API 镜像为 `sha256:2cec7e3bfc43dbb3841c246595d167621adb1012b9662461be763e8b2096bc16`；前端主资源为 `index-D03-RU0p.js`。上线后首页、资源、CAD 能力、匿名隔离、管理员登录与身份/项目查询均通过。相关回归 157 项及最终裁图专项 18 项通过。

实测续接 `cad_3f57c3bde8a346cd9e64d1341abbed8d` 在 15:15:26 UTC 启动，保留 1 个草稿特征和 6 项观测，首个规划动作 medium、180 秒上限及 120 秒构造/复核预留均生效。没有再裁图，约 188 秒后询问上部结构 Y 向深度和台阶方向。问前独立复读成功，已识别 20/15 是竖向差，但没有联结原图 35 与 R33 推导 Y 向范围；复合问题因此整体未解决。这属于仍存在的读图推导限制，不能称为全自动建模验收通过。

诊断时通过正常聊天补充原图已有关系：以后缘为 Y=0，主孔中心 Y=35、前圆 R33，因此上部范围 Y=0…68；最高面 Z=75，前面 Z=55，槽底 Z=40。未修改原设计或直接写入 CAD 计划。续接 `cad_f8cea8e9be6a4b22b6e2e060ed1c2672` 在 15:19:02 UTC 启动，模型服务先在约 61 秒返回 HTTP 504；自动重试又在约 73 秒返回 SSE `response.failed / upstream_error`，没有任何完整正文。任务约 141 秒失败，正确保留原草稿及观测，没有执行不完整 JSON，也没有伪造完整实体或交付通过。当前完整 9.jpg 模型尚未生成，剩余阻塞包括上游服务不稳定以及自动空间推导能力；不能仅以服务健康或单元测试通过宣称整条建模链路已恢复。
