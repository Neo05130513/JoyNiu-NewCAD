# 变更记录

## 2026-09-13 · 恢复 JoyNiu CAD 品牌名

- 首页侧栏与三维编辑器顶部改回“JoyNiu CAD”，同步首页无障碍标签和浏览器标题。
- 构建及桌面/390 px手机实际页面检查通过；此条记录为仓库修改，既有r8发布哈希及历史截图保持原始证据。

## 2026-09-13 · 完整三维图形编辑 r8（已发布）

- 补齐多轮廓/孔洞、样条与约束、任意扫掠/放样、参考几何、曲面、PMI、实体操作、原位导入/标准件、PDM及资源社区。工作区采用工具栏、特征树、画布、图档结构，桌面和手机浮窗实测通过。
- 前端784项、相关几何/持久化后端282项、发布与恢复167项通过；Linux完整候选及公网鼠标编辑、STEP回读、跨账号副本、撤下和重新登录重开通过。
- 已发布r8，客户保存数据保留；三个测试账号停用、临时资源撤下。HTTP刷新需重登录的现有策略保留。详见[最终发布记录](currentcad-editor-release-r8-2026-09-13.md)。

## 2026-09-12 · 用户视角全流程测试与修复（本地验收）

- 实际浏览器执行新建、特征编辑、原生绘图、STEP 上传测量、真实装配、工程图、照片标定、标准件、任务与账户页面；检查桌面和 390 px 手机布局。
- 修复切页丢草稿、保存范围误导、标准件装配跳转错误、小数输入和撤销同步、失效测量残留、移除视图误删尺寸、照片错误与缺项处理；隐藏工具暂停渲染和快捷键。
- 工程图预览支持独立缩放，修正主按钮悬停对比度；PDF 实际嵌入中文字体并经双阅读引擎复核。未配置模型服务明确显示未调用，而非无效模型结果。
- 前端 490 项、全量后端 2074 项通过；最后 PDF 修复后 12 项专项通过，稳定构建通过，STEP/DXF/BOM 实际回读一致。未发布生产，隔离预览 AI 接入仍未配置。详细范围和边界见 [用户视角测试记录](user-experience-qa-2026-09-12.md)。

## 2026-09-12 · 线下充值、官方价折算和运行保障（已部署，收费关闭）

- 线下收款充值单、全局唯一凭证、事务账本、全额线下退款、客户查账；赠送/补偿分开。新购买使用独立默认关闭开关。
- 固定10元1000积分；后台官方Standard API价格折算、可调倍率与结算汇率，版本随任务保存。仅成功候选轮次收费，超余额记待补缴；查看原图补定位免费。
- 客户帮助、数据删除申请/人工回执、交接指南和条款草稿；8份已有STEP离线回读验证。
- 宿主定时监控、协调备份、受保护后台报告与恢复工具。AI接入、正式访问与账号安全按用户要求暂缓。
- 504项后端和414项前端通过，后续运维132项专项通过，构建及隔离镜像HTTP启动通过；`offline-credits-20260912` 已部署，公网管理员浏览器和1039文件/2库的隔离恢复通过，修正恢复探针路径误报。完整上线记录见 [本轮交付记录](commercial-delivery-2026-09-12.md)。未发布占位条款，不代客户同意，不提前启用实际扣分。

## 2026-09-12 · CurrentCAD 能力补全（本地验收完成）

- 按用户授权补全原生二维、直接特征编辑、真实装配、通用工程图及专业工具；排除实时共编、小程序与密码受限分享。
- 以对方成功率 100% 为规划假设，功能与几何验收按实际结果记录，保留现有交付门禁。
- 已落地原生图元与关联标注、参数约束/图库、气泡检验图/XLSX、特征草图编辑、齿轮/弹簧等内核能力、STEP 测量、真实配合/干涉/BOM、AI 多零件装配、任意剖视、照片标定、20 个专业构件与 12 个真实案例。
- 446 项前端、2062 项全量后端通过；12 案例真实 STEP 回读、实际 AI 装配与带图阶段调用、桌面及手机流程通过。后续原生二维/DWG/气泡 42 项通过；Linux ARM 静态工具链与无编译器运行容器真实 DWG 往返通过。未由本任务发布生产。
- 功能、边界和可下载文件：[CurrentCAD 补全记录](currentcad-completion-2026-09-12.md)。


## 2026-09-12 · 确定线下收款交付方式与差距清单（需求与审计）

- 当前采用线下收款、后台向客户充值积分；线上购买保持关闭，客户仍登录网站自助建模。产品规格新增当前有效方案，替代现阶段上线线上购买的历史安排。
- 实时确认生产线上购买和实际扣费均关闭、计费规则和正式条款为空；记录全局并发 1、队列 20、HTTP、磁盘 83% 及未配置外部告警等实际状态。
- 区分已有积分调账与待补的线下充值单，列明收款凭证、重复入账、冲正、线下退款／发票和独立购买开关等缺口；整理首批交付顺序及可后置功能，见 [客户交付差距清单](customer-delivery-readiness-2026-09-12.md)。
- 本次仅更新需求和审计文档；未运行模型、充值、扣分、支付或退款，未变更管理员密码／模型／生产配置。独立验证会话已退出，不将待实现项目描述为已经上线。

## 2026-09-11 · 管理员账号名登录（已部署）

- 登录页支持账号名或邮箱；注册及邮件找回仍要求邮箱。增加唯一登录别名，保留原账号 ID、邮箱、角色和项目归属，不内置任何通用管理员或默认密码。
- 别名分配在事务内核验当前操作人权限并写审计；修改别名和重置密码撤销原有会话。非法／未知别名与错误密码统一拒绝，原邮箱登录继续兼容，注册和密码长度规则未降低。
- 按用户指示为现有线上管理员设置登录别名并重置密码；密码仅以带盐哈希保存，不写入代码或发布文档。公网真实浏览器完成账号名登录、管理页访问及退出验证，验收会话已撤销。
- 7 项前端回归、31 项既有账号／角色回归及 9 项别名专项通过，构建、隔离镜像启动和 14 项公开检查通过。发布镜像 `admin-login-20260911`：API `d5994b1d0822`、Web `101674698cdb`；停写备份 `/opt/joyniu-backups/pre-admin-login-20260911` 已校验（1008 文件、2 库），上一版 `admin-v2-20260910` 保留。模型配置和收费开关未调整。

## 2026-09-10 · 管理后台运营能力增强（已部署，收费关闭）

### Added

- 将后台重组为运营工作台、交易与积分、平台配置三组，共 16 个可见菜单；新增按权限限定的顶部快捷搜索、侧栏收起和小屏导航。财务拆分为充值订单、积分流水、任务结算、退款、发票、模型用量、积分套餐七个独立页面，旧账务地址保持兼容。
- 运营总览增加北京时间 7/30/90 日真实趋势及每日明细；客户档案支持企业、联系人、电话、标签、内部说明维护与不可改删的追加跟进，关联该客户的业务查询入口。
- 建模任务增加独立人工处理状态、优先级与内部备注，提供相关筛选、中文排障建议和白名单诊断导出；人工标记不改变 CAD 状态，不触发 AI 重试。
- 财务查询增加适用页面的服务端客户、编号、状态／类型、起止日期筛选，分页计数同步；新增管理员积分流水和明确范围的当前页 CSV 导出，处理表格公式注入风险。
- 客服增加全部待处理、分配给我、未分配队列，负责人／优先级分派及筛选；客户沟通与后台内部备注独立保存，常用回复只填入草稿。工单深链查询参数真实生效；分派与备注沿用版本、幂等、即时权限校验和不可变审计。

### Fixed

- 运营账号统计区分全部账号、业务客户、后台成员；待处理工单只包含 open/in_progress，不把 resolved 算作积压；待确认、待补充任务不计为完成，未知用量不按零计算。
- 财务日期按 Asia/Shanghai 整日边界处理，混合时区时间戳按实际时间排序；筛选与分页返回一致结果。
- 修复重复点击同一工单筛选时列表可能停在空白、切换工单后迟到请求覆盖当前详情，以及小屏导航遮挡问题；保护私有备注与客户消息隔离。

### Verification and release status

- 本轮前端现有全量测试命令 403 项通过，相关后端回归 454 项通过；后者不是全 CAD 几何测试的穷尽执行。财务新／扩展 61 项及既有 21 项通过，客服后端 27 项、前端 12 项通过，子集数量不与总数重复相加。
- 本地 1440×1000 浏览器验证 CRM 保存及 HTTP 复查、追加跟进、人工任务状态／备注且 CAD 状态不变、七个财务页面查询、订单无结果筛选、工单分派／优先级／内部备注持久化和模板只填草稿。390 px 小屏修复后无页面横向溢出；恢复原 QA 资料与安排，追加内部验收记录保留本地。
- 未写入线上客户业务，未调用模型、付款、退款、扣积分或站外消息。费用预估、积分冻结与预算上限暂停仍排除，商业开关保持关闭。
- 已发布 `admin-v2-20260910`：API `0f7e32891044`、Web `fea6a11ce130`，API healthy。停写备份 `/opt/joyniu-backups/pre-admin-v2-20260910` 已校验（998 文件、2 库）；17 个历史任务及既有业务表记录保留，699 个数据文件哈希不变。14 项公开检查及 57 项管理员只读检查通过，独立验收会话已撤销；旧 `admin-20260910` 镜像保留回滚。详见 [后台运营增强与验收](admin-operations-v2-2026-09-10.md)。

## 2026-09-10 · 独立管理员后台（已部署并验收，收费关闭）

### Added

- 增加独立 `/admin` 后台与登录入口，集中运营概览、客户管理、建模任务、积分订单、客服工单、计费规则、账号权限、服务条款、系统状态及统一审计。页面支持地址直达和浏览器前进/后退；返回设计工作台保留当前模型、聊天与未完成任务状态。
- 增加运营 `ops`、财务 `finance`、客服 `support`、审计 `auditor` 业务角色，并在服务端接口/服务方法与前端页面同步限制权限。运营可诊断和停止任务；财务可核账、退款和调账；客服可处理工单和读取安全任务摘要；审计只读。套餐价格、计费规则、正式条款与账号角色仍仅全权管理员可改，工程角色不自动获得后台权限，保留最后管理员和会话撤销保护。
- 增加跨客户任务/账号搜索、状态筛选和分页，展示真实任务状态、独立调用用量及有权限的财务信息。兼容尚无新任务登记记录的历史 `cad_runs`，不显示客户原图、对话、模型代码、原始异常或签名下载链接；历史任务不具备在线取消条件时明确不可停止。
- 增加带原因与幂等编号的管理员停止请求，队列状态与审计同事务更新，等待工作进程真实结束后才显示已取消。统一读取账号、业务、财务和工单审计，并为客户/任务详情访问记录操作人、对象、时间及财务可见性。

### Fixed

- 修复后台分页及账号状态/审计操作人筛选条件在路由切换时丢失；拒绝额外未知路径段，越权深链显示明确提示。隐藏的设计工作台不再触发可视子组件的快捷键、弹窗、焦点和滚动副作用。
- 修复 macOS 发布包中的 AppleDouble `._*.json` 被 OCR 样例加载器作为 UTF-8 JSON 读取、使平台初始化失败且平台/CAD/后台路由缺失的问题。发布打包禁用资源叉与扩展属性，构建上下文和镜像应用目录过滤该类文件；样例加载器跳过隐藏元数据，真实损坏 JSON/UTF-8 继续报错。

### Deployment and verification

- 首次切换遇到上述元数据污染后已回滚旧镜像恢复服务；完成打包、镜像与加载器修复，经独立镜像启动验证后重新发布。当前 API 为 `joyniu-cad-api-codex:admin-20260910`（`083f5617b04d`），Web 为 `joyniu-cad-web:admin-20260910`（`a12071bb896e`）。
- 最终停写备份 `/opt/joyniu-backups/pre-admin-20260910-final` 已校验，包含 1188 个文件和 2 个数据库。旧数据保留，没有清库或重新创建客户项目。
- 前端 380 项、后端 269 项测试全部通过，后端数量已核对 JUnit XML。线上 `verify.py` 的 14 项公开检查和额外 14 项管理员 API 检查通过，覆盖未登录拒绝、历史任务、客户/任务关联、审计、安全响应字段、筛选分页及关闭收费状态；验收会话已撤销。
- 线上 API healthy、Web 运行，Codex 登录状态为 `Logged in using ChatGPT`，当前配置为 `codex / gpt-6-astra / high`。两个数据库 `quick_check=ok`，保留 17 个旧任务，`cad_job_attempts=0`，镜像代码树 `._*` 文件数为 0。线上 `/admin` 展示独立登录且没有注册入口；已登录后台交互另在本地浏览器验证。实际范围见 [管理员后台验收记录](admin-acceptance-2026-09-10.md)。
- 发布后必须分别确认平台、CAD 和管理员路由存在且行为正确，不能只检查核心 `/api/v1/health`。具体核查与回滚过程见 [商用发布说明](../deploy/commercial_release.md)。
- 购买支付、实际积分收费和真实退款执行仍未启用；本次不生成 AI 任务，不改变模型、Codex 登录、出口或既定计费规则，不增加费用预估、积分冻结或预算上限暂停。

## 2026-09-10 · 在线积分服务一期（已部署，收费关闭）

### Added

- 完成账号独立云工作区、服务端修订冲突保护及按账号/API 地址隔离的浏览器草稿；完整保存项目、聊天、参数和文件版本。公共旧工作区必须由客户显式确认归属导入，资格及进度跨刷新保留，不自动混入登录账号。
- 完成注册开关、会话续期/撤销、改密、账号切换隔离和运营用户管理；管理员角色、停用及重置密码留痕，保护最后一个启用管理员。邮箱验证和邮件找回未接入。
- 完成客户任务中心、分页及状态筛选、全局和单账号队列容量、请求幂等、断线恢复和真实取消；在途 HTTP 请求仍保持取消请求中，等待其实际结束，不假报停止或成功。
- 接入供应商调用编号和执行尝试计量，固定规则/条款快照并在 attempt 终态封存调用清单、汇总结算一次；缓存输入和推理 token 不重复加计，缺失用量保持待核账，恢复结算不重复扣分。商业开启时关闭未接入计量的旧通用 AI 入口。
- 完成事务钱包、实际扣分、补偿、结算列表、套餐/订单/流水、管理调账与成本记录；增加规则版本管理及公开已启用费率展示。当前套餐价格、费率及收费终态均为空，收费默认关闭。
- 实现余额不足两种可选策略：待补缴 `deferred_due`，或最多扣至零并由平台承担差额 `cap_at_balance`。无默认选择；待补缴优先由后续入账清偿，未清偿时阻止新收费任务。
- 完成微信 Native 实际适配器、验签解密、幂等入账及主动查单；增加退款/发票申请、审核、整单原路退款提交、查询与通知处理。渠道结果未明确成功时不标退款成功，真实退款执行默认关闭；支付宝及自动开票未接入。
- 完成三类正式条款的后台发布、版本留存、客户主动接受与订单/任务收据；正文目前为空，缺少正式文本或必要接受记录时阻止相应购买和收费操作。
- 完成自包含 HTML 核对报告，默认只导出当前已确认且实体就绪的结果；列出中文参数、单位、名义/派生值、原图来源、已测/未测、未决问题与确认记录，不作制造保证，不携带访问凭据。
- 完成真实数据库支持工单：客户新建、查看、追加、关闭，管理员回复与状态流转，以及分页、权限、幂等和审计。关联任务须归属本人，图纸授权默认否且可撤回；仅记录授权，不开放管理员 CAD 图纸下载，无邮件/短信通知或时效承诺。
- 增加文件版本绑定的限时签名访问、本人撤销和停用拒绝；旧永久令牌默认禁用且商业模式强制禁用。完成完整数据与 SQLite WAL 备份、校验及隔离目录恢复工具和本地演练说明。

### Fixed

- 防止账号切换、令牌刷新、StrictMode 和迟到异步响应覆盖另一账号；非持久登录到期退出并保留账号草稿。修复云端空项目自动保存后旧项目导入入口丢失，以及跨标签刷新竞争。
- 订单幂等键不因访问令牌刷新而重置；任务在仅有 requestId 时仍可恢复已接收请求，排队响应继续轮询，取消失败保留真实状态。管理员和客户端异步操作均按账号隔离并防重复提交。

### Scope and pending configuration

- 保留用户明确拒绝的三项：建模前预计积分/最高消费提示、任务积分预占冻结、预算用尽暂停并要求同意继续。未加入预算流程；计费时机已经确定为 attempt 终态，两种不足策略仍待用户明确选择。
- 微信代码尚未经过真实商户付款和退款验收；正式条款正文、套餐价格和收费规则仍未提供。正式商用模型授权未核实，自动异地备份及告警未开启；正式域名/HTTPS、实际数据处理约定和生产验收仍需完成。
- 本轮已部署禁收费版本；保留旧镜像，完成 931 文件完整备份、两库隔离恢复迁移与原表记录数校验，另存一份本机备份。线上已有 17 次任务保持可读，抽查原图哈希一致。保留 `Product-Spec.md` 后续全部历史功能规格及既有高级功能，不将本轮完成代码表述为已经开售。

### Verification

- 各模块专项自动化测试、前端构建和本地界面检查已执行；其中支持工单后端 20 项、支持/工作区同步/积分客户端/核对报告前端 41 项测试通过，构建通过。完整发布验收以发布记录为准。
- 上述本轮验证未发起真实付费模型调用或商户付款，不替代生产部署、外部安全审计、容量及异地恢复验收。发布和备份流程见 [商用发布说明](../deploy/commercial_release.md)。

## v0.5.2 · 2026-09-04 · DWG 原生解析与矢量量测规格

### Added

- 接入 GNU LibreDWG `dwgread`/`dwg2dxf` 转换与 `ezdxf` 原生尺寸/矢量解析；新增 `POST /api/v1/dwg/inspect`，可独立返回 DWG 版本、单位、布局、实体统计、尺寸对象、矢量坐标以及 DXF/PNG 派生产物哈希。
- AI 对话的 DWG 上传改为本地只解析一次，再把高清 PNG 与受限矢量摘要复用于提取、审校和裁决阶段；原始 DWG 二进制不会进入中转站请求。转换失败会返回独立错误码，且不会调用远程 AI。
- 为验收文件 `1(1).dwg` 新增 `stepped_tapered_nozzle_with_insert_v1`：主件为 98 mm 阶梯锥形空心回转体，右侧 Ø39.4×40 M12 镶件保持独立第二实体。CadQuery/OCCT STEP 回读保持 2 个 solid，GLB 保持 2 个具名 mesh/node。
- 参数面板、特征树、AI 候选同步、人工确认、通用模型生成、PDM 一体化流程与导出均支持该新配方；界面显示 `direct_dimension`、`vector_derived`、`ai_interpreted`、`manual` 来源及末段长度、出口锥深和径向间隙等派生值。
- M12 在当前图纸缺少螺距与公差等级，因此只生成名义 Ø12 直孔并显示非真实牙型警告，不伪造可制造螺纹。
- 新增 `evidence_only` 矢量候选层：验收 DWG 可追溯读取 14 个原生标注字段，并用 Ø13、Ø17、7.4641 mm 轴向长度及两条锥线交叉量算 15°；图纸未定义的 `insertAxialOffset` 保持 unresolved，等待远程模型提出可人工确认的语义候选。
- 修复远程提取成功、后续审校返回坏 JSON/网络错误时清空整份候选的问题；现在保留已通过白名单校验的模型候选，明确标记 `reviewComplete=false` 与错误类别，并继续允许客户逐项人工确认。
- 修复前端对 snake_case、数组证据和数值 `0` 的同步遗漏；上传 DWG 后的新配方不会再被旧模型的并发修订保护错误拦截。真实 GLB 保留主件/镶件各自材质色，剖切时可区分组件。

### Specified

- 将 DWG 从“允许上传并直接作为 `input_file` 转发”升级为原生解析输入：先由部署侧 DWG SDK/转换器读取版本、空间/布局、图层、块/插入变换、尺寸实体和矢量几何，再生成规范化矢量中间件、高清渲染和受限结构化摘要。原始 DWG 二进制不再作为远程模型可见文件。
- 建立字段级来源模型：原生尺寸 measurement 为 `direct_dimension`，图元端点/圆心/半径等确定性量测为 `vector_derived`，远程模型的视图/拓扑解释为 `ai_interpreted`，客户修改为 `manual`。每个值保留文件哈希、布局、图层、entity handle、坐标系、单位、变换、公式和确认状态。
- 明确模糊标注的处理：优先读取关联 `DIMENSION` measurement；缺失时允许由矢量几何量测，但必须展示图元证据、单位、坐标系、公式与容差，并在人工确认前阻断实体生成。
- 明确二维到三维边界：解析出完整 2D 尺寸并不保证唯一 3D 拓扑；仍需匹配已审核配方/特征树、人工确认和 OCCT/PDM/CAM 生产门禁。

### Security and failure states

- 新增转换器缺失、不支持版本、损坏/加密、XREF 缺失、单位不明确、转换超时、文件超限和图元超限等独立错误契约；DWG 预处理失败不再显示成泛化的“远程大模型未形成候选”，且失败前不调用远程 AI。
- DWG 解析/转换必须在隔离临时目录和资源受限子进程中执行，禁止宏、AutoLISP、VBA、OLE、嵌入程序、外部命令和非上传范围的网络/路径访问；明确单件大小、CPU/内存、时间、布局、嵌套块、图元与渲染像素限制。
- 远程 AI 只接收高清渲染和字段白名单内的有限摘要；本地 `direct_dimension`/`vector_derived` 数据不得伪装成 `ai_interpreted` 或“AI 已识别”。

### Acceptance fixture

- 新增用户文件 `1(1).dwg` 验收夹具契约：AC1021（AutoCAD 2007/2008/2009）、114,016 bytes、SHA-256 `5c3300b60c9e7c5bfe6dc839c525dd9b9fc4d24175388af38c09b698880b2944`。验收覆盖版本/布局/单位报告、原生 DIMENSION 证据、至少一项矢量派生量测、高清渲染、受限 AI 摘要、人工确认和未知三维拓扑门禁。
- 上述转换器、解析 API、证据面板、新零件配方、OCCT 双实体、人工确认及 PDM 分发均已实现并由自动化测试覆盖；任意未知 DWG 仍必须先映射到已审核配方，不能仅凭二维包络伪造生产三维体。

## v0.5.1 · 2026-09-02 · 远程多阶段审校与真实失败状态

### Added

- 图纸上传改为远程提取 → 远程尺寸链审校 → 复杂配方无候选数值盲审 → 相邻位置尺寸多次独立专项复核；每轮都重新查看原图，专项复核使用无 OCR/无模板值的高清图像块，至少两票一致才写入，前端只收到最终白名单候选。
- 高度尺寸链增加独立远程多票复读，分别锁定 `pedestalHeight=40`、`rearClampRise=20`、`boreFloorZ=40` 和 `crossHoleCenterZ=55`；共识按字段计算，单个字段无两票一致时仅移除该字段。
- 增加上游 `timeout/transport/incomplete/invalid_json/empty_response/invalid_stream`安全分类、尝试次数和无敏感内容耗时日志；完整 JSON delta 已到达时可从意外中断的 SSE 恢复。

### Fixed

- `local-fallback` 不再产生空 evidence、候选卡或“确认数据”入口，也不再把上一个动力轴冒充为新图纸候选。原图保留可重试，旧模型明确标记为上一版本。
- 远程结果未给出 `parameterEvidence` 时显示“待评估”，不再把本地 Tesseract 的低置信度显示为 AI 置信度。
- 根据 `9.jpg` 俯视图尺寸界线纠正两个纵向基准：圆筒轴心距后缘 35 mm，安装孔中心线距后缘 40 mm。
- 纠正实体拓扑：下座改为前半 R33、后半直边延伸至后缘的 D 形；后高墙改为矩形直边，筋板移到后缘 10 mm 带，底板后角保持方角，横孔切通后侧。

### Verification

- 完整 FastAPI/CadQuery 测试集、前端生产构建和 diff 校验通过；真实 `9.jpg` 默认短提示词实测经 7 次 Responses SSE 远程分析返回全部 25 项参数，人工确认后生成一个有效 OCCT solid 与生产级 STEP，包络为 `125×95×75 mm`。

## v0.5.0 · 2026-09-02 · 开口夹紧座通用配方与真实实体验收

### Added

- 新增 `split_clamp_support_v1` 服务端参数配方，对应 `partType=split_clamp_support`。标准实体包络为 `125×95×75 mm`，包含底板主体与前舌、R5/R8 圆角、R33 圆筒座、Ø36 中央盲孔、12 mm 径向开缝、2×Ø12 安装孔、Ø12 横向孔和对称加强筋。
- 新增白名单配方调度器与通用几何入口 `POST /api/v1/models/validate` / `POST /api/v1/models/generate`。两个入口均校验 `partType` 与 `recipeId` 的对应关系，并严格拒绝未知参数。
- 新增开口夹紧座专用 CadQuery/OCCT 建模、审计和 fallback 草稿。STEP 与 GLB 来自同一配方；生产验收检查单实体、包络、外圆柱、中央孔、安装孔对、横向孔、开缝和底板圆角。

### Changed

- 多模态候选结果增加零件身份契约：远程模型可同时返回 `partType`、`recipeId`、`parameterPatch` 和字段级 `parameterEvidence`。开口夹紧座候选返回后，工作台立即同步对应的参数面板、特征树和三维草稿，不再继续显示轴类或旧支架模型。
- 候选数据保持 `pending/needs_review`；客户可编辑后显式确认。确认提交携带 `partType`、`recipeId` 和 `parameterOverrides`，生成时使用服务端登记的已确认 `sourceDrawingId`，并拒绝与确认快照不一致的参数。
- 按俯视图尺寸界线重新核对纵向基准：圆筒轴心距后缘 `pedestalCenterFromRear=35`，安装孔中心线距后缘 `mountHoleCenterFromRear=40`；横向孔中心为下座顶面 `crossHoleCenterZ=15+40=55`，而非 65。缩颈凹角为 R5，前舌和底板外凸角为 R8。
- 旧 `/brackets/validate` 与 `/brackets/generate` 保留兼容；支持多配方的工作台改用通用 models 入口。

### Verification

- 增加候选聊天 → 客户确认 → 通用实体生成的端到端覆盖，验证已确认图纸 ID、配方身份和参数快照能连续交接。
- 增加配方不匹配、未知字段、参数关系冲突和严格 CadQuery 禁止降级的回归覆盖。
- CadQuery/OCCT 可用时，覆盖 STEP 回读：回读后仍为一个有效实体，包络保持 `125×95×75 mm`，关键拓扑审计继续通过；GLB 检查 `glTF` 文件头、版本 2 和字节长度。

### Production boundary

- `requireCadQuery=true` 时不得降级；CadQuery/OCCT 缺失必须明确失败。faceted/WebGL fallback 始终为 `productionReady=false`，只能审阅。
- 客户确认只解锁参数实体生成，不替代 OCCT 校验、STEP 回读、PDM 发布、CAM 审批或 NC 放行。

## v0.4.1 · 2026-09-02 · 图纸候选同步预览

### Fixed

- 上传图纸后只要远程大模型返回有效候选，右侧参数面板和中间三维草稿会立即同步，不再停留在上传前的轴类示例模型。
- 冲突检测改为客户操作修订号，内部分析与渲染状态不再误报为“回复期间人工编辑”。
- 仍保留候选字段来源、待补全提示、人工确认与 OCCT 生产门禁。

## v0.4.0 · 2026-09-02 · 可持续交互的 CAD Copilot

### Added

- AI 设计助手升级为真正的多轮聊天线程：显式携带历史消息，不依赖中转站响应存储即可理解后续追问。
- 新增浏览器 SSE 对话通道，支持阶段状态、AI 文本增量、最终结构化 CAD 结果和安全错误事件。
- 聊天输入支持 Enter 发送、Shift+Enter 换行、随消息附加图纸、停止当前生成和新建对话。

### Changed

- AI 回复先在同一消息气泡中流式呈现，最终结果到达后再一次性应用白名单参数补丁；取消或失败不会提交半成品 CAD 修改。
- 新图纸继续进入人工确认，纯文字二次修改继续沿用参数校验、OCCT 重建与生产导出门禁。

### Verification

- 覆盖多轮历史输入、SSE 事件顺序、最终结果单次提交、取消不应用补丁以及浏览器连续对话交互。

## v0.3.3 · 2026-09-02 · 大模型唯一候选源与 Responses SSE 流式分析

### Changed

- 中转站调用统一发送 `stream=true` 并按 Responses SSE 事件消费结果；服务状态增加 `streaming=true`，中转站后台不再把图纸分析记录为非流式请求。
- 图纸重试改为两次真实的多模态模型调用：首次以 `high` 细节读取原图，兼容重试以 `low` 细节读取同一原图；每次拥有独立的网络超时，不再出现未实际发起第二次调用却显示“已重试”。
- 上传图纸的自动候选只接受远程模型 `parameterPatch`。OCR、哈希校准和几何解析继续保留证据，但不再合并、补齐或覆盖 `candidateParameters`。
- 移除图纸轮次的本地中文参数解析和校准配方替代路径；远程模型未返回的字段保持空白，由客户人工补全，不再使用模板默认值冒充 AI 数据。
- 视觉默认分辨率从 `1200/low` 调整为最大边 `4096/high`；兼容重试仍显式使用 `low`。

### Verification

- 新增 Responses SSE `delta/completed` 解析、上游 `stream=true`、高清→低细节同图重试和独立超时测试。
- 新增“本地校准完整但远程 patch 为空”“OCR 明确识别 999 但 AI patch 为空”回归测试，确认 API 候选保持为空。
- AI 代理测试 28 项通过，前端生产构建通过。

## v0.3.2 · 2026-09-02 · 候选参数默认值回填

### Added

- AI/OCR 只返回部分尺寸或没有可靠尺寸时，支架的 14 个必填字段会用完整、可编辑的模板默认候选补齐，不再显示一整页空白输入框。
- 参数候选增加字段级来源状态：图纸识别、AI 候选、模板默认和人工修改；分析摘要同步显示明确候选与模板默认的数量。

### Changed

- “确认数据”现在确认页面中可见的完整参数快照；模板默认值无需客户逐项重新输入，但仍必须经过一次显式确认和服务端几何校验。
- 模板默认候选只用于当前受支持的零件模板，不计入识别证据或置信度，也不会在确认前触发实体生成或生产导出。
- 远程 AI 空响应、不完整响应和瞬态 4xx/5xx 会在同一总超时内自动进行一次无历史、无存储的兼容重试；失败提示不再误称整个中转站不可用。
- 中转请求不再发送 `max_output_tokens`，也不再限制消息或模型状态的输入长度；输入上下文与输出长度完全交给所选模型和中转站。
- 本次远程分析降级时保留原始附件，工作台会显示“重新尝试 AI 分析”，并明确区分“中转站在线”和“本次请求已降级”。

### Verification

- 覆盖空 AI patch、部分 AI patch、人工覆盖默认值、确认前阻断以及完整快照确认五类回归场景。
- 覆盖空结构响应、不完整响应、503 重试、401 不重试、八位流图片 MIME 修复以及降级文案回归。

## v0.3.1 · 2026-08-31 · AI 候选分析与客户确认闭环

### Added

- 新增独立的 `POST /api/v1/drawings/{drawingId}/accept`（及兼容路径）：客户/设计师可提交当前编辑后的 `parameterOverrides`，服务端完成严格参数/几何校验并记录 `confirmationType`、`confirmedBy`、`confirmedAt`。
- AI 图纸响应增加 `candidateParameters` 候选包；未知或低置信图纸也会返回可编辑候选、来源、置信度、假设、特征和问题，不再只有“复核”状态。
- 设计工作台增加“AI 分析摘要”和明确的六阶段路径：上传 → AI 分析 → 确认数据 → 生成 3D → 二次修改 → 导出。

### Changed

- 上传图纸（包括哈希校准图）只完成分析，不再隐式生成实体；“确认数据”和“生成 3D”是两个独立按钮。
- `needs_review` 的产品语义改为“候选数据等待人工确认”，不再作为 reviewer-only 的终点；客户可在参数面板补全/修改后继续。
- 上传新图纸会立即重置流程轨和参数证据状态：上一版本只能作为明确标记的预览，不会让新图纸误显示为已分析、已确认或已生成。
- 多模态代理提示词要求模型分析所有可见视图和标注，返回能证明的完整候选字段；不确定项必须列入问题，候选不会被冒充为生产真值。

### Verification

- 后端全量测试、严格图纸验收和前端生产构建通过；unknown candidate → explicit accept → geometry hand-off 有回归覆盖。

### Known limitations

- 任意未知图纸仍需要人工确认其候选拓扑；如果模型/ OCR 无法形成足够字段，界面会要求补全，而不是猜测并生成错误实体。
- PDM 正式发布、CAM 审批和 NC 放行仍由 reviewer/manufacturing RBAC 控制；客户确认只解锁当前设计实体生成。

## v0.3.0 · 2026-08-30 · 客户工作流与 CurrentCAD 风格工作台

### Changed

- 将客户主路径收敛为单一六阶段工作流：输入/上传 → 识别尺寸 → 复核证据 → 生成实体 → 二次修改 → 导出交付；3D 工作台顶部固定显示当前阶段和下一步主操作。
- 首页增加首屏“上传图纸开始分析”主入口；3D 工作台上传改为先进入待处理队列，客户可补充意图后点击“开始 AI 分析”，避免选择文件后隐式覆盖对话内容。
- 证据摘要、模型上下文和交付状态在同一页呈现；未确认图纸、过期参数和非 OCCT 预览均使用明确的阻断/提示文案。
- 重复上传时旧实体会自动降级为“上一版本预览”，不会继续显示为当前可交付结果；未知/低置信识别的数值标为候选值，必须经 reviewer 对照原图确认。
- 合并导出入口为右上角“导出交付”菜单，集中提供 STEP、GLB、DXF 和参数 JSON，避免视口、检查器和项目卡重复出现同一下载动作。
- 重排导航层级：设计入口优先，高级 PDM / 账号 / CAM / NC 放入“高级”分组；侧栏和顶部导航在窄屏保持可用，不再显示竖排文字或隐藏全部工作区标签。

### Verification

- 视觉验收覆盖 1280×720、1024×768 和约 489px 窄屏：上传、步骤条、3D 视口、检查器和主操作均可定位。
- 保留 v0.2.1 的真实图纸链路：验收支架图经 OCR/证据确认后仍生成 CadQuery/OCCT STEP + GLB，并支持后续尺寸对话修改。

### Known limitations

- 任意未知图纸仍需显式人工确认；v0.3.1 将确认动作前移到客户设计工作台，不再要求跳转 reviewer 登录页面。
- 2D/装配高级工作台仍是独立能力，后续将继续把其模型上下文与当前项目版本完全绑定。

## v0.2.1 · 2026-08-30 · 多模态 AI Copilot

### Added

- 新增服务端 AI Copilot 代理：`POST /api/v1/ai/conversation` 与兼容别名
  `/api/v1/ai/chat`，以及不暴露密钥的 `GET /api/v1/ai/status`。对话可携带当前模型状态、上一轮 `previousResponseId`和多个附件。
- 支持图片、PDF、DXF、DWG 多模态输入。图片以 Responses `input_image`
  发送，PDF/DXF/DWG 以 `input_file` 发送；最多 4 个附件，单件上限 20 MiB。
- 默认使用 `gpt-5.6-sol` 与 `reasoning.effort=high`，输出仅包含说明、问题、审查状态和参数白名单内的 `parameterPatch`。通过白名单、类型、有限值和正数边界校验后才能更新 CAD 模型。
- 对验收支架的已确认图纸或明确尺寸修改，三维工作台可在补丁通过几何校验后自动请求 STEP + GLB，并更新参数面板、特征树和预览。
- 远端 AI 不可用时增加可解释的本地路径：哈希校准的验收图和匹配明确中文参数可继续应用；未知图纸仍保持人工复核。

### Security

- AI 供应商凭证仅从 API 服务端的 `JOYNIU_AI_API_KEY` 或
  `JOYNIU_AI_API_KEY_FILE` 读取（`JOYNIU_LLM_*` 为兼容配置），不进入前端 bundle、localStorage 或响应体。
- AI 对话默认需要 Bearer token 和 `ai:chat` 权限，仅 designer/admin 可用；匿名演示必须由本地部署显式开启。供应商原始错误不转发给浏览器。
- 本地匿名 AI 入口进一步限制为 loopback 或显式 development/local 环境；未知图纸的复核状态会跨后续纯文字对话保留，未注册的兼容识别 ID 不会作为可生成来源返回。
- 附件会传至部署所配置的远程 AI 端点；生产部署必须在数据分类、传输、保留和供应商合规完成后才能上传保密图纸。

### Changed

- AI 对话改为“参数补丁 → 几何校验 → 实体生成”的闭环；参数变更会将旧 STEP/GLB 标记为过期，失败或待复核时不展示为生产交付。
- 产品规格升级为 v0.2.1；原 v0.2.0 图纸几何验收尺寸、OCCT 生产边界、PDM/RBAC 和 CAM/NC 门禁保持不变。

### Verification

- Copilot 接口覆盖多轮 `previousResponseId`、模型状态传递、附件类型/大小限制、响应白名单和无效补丁拒绝。
- 验收图的哈希证据可与对话补丁合并；在 CadQuery/OCCT 环境中，通过校验后自动产生 `productionReady=true` 的 STEP 与 GLB。

### Known limitations

- 多模态 AI 仅是参数化轴类/支架编辑助手，不是任意图纸的通用 CAD 重建器。未知图纸不会自动确认或生成虚构几何。
- PDF/DXF/DWG 当前以文件输入转发，不保证原生 DWG 实体解析、所有 DXF entity 拓扑重建或多页 PDF 视图对齐。
- 没有 CadQuery/OCCT 时的 faceted 文件只能审阅；任何 AI 回复、GLB 或 fallback STEP 都不能替代 OCCT 回读、PDM 版本和 CAM 审批/放行。
- 远程模型的延迟、限流、成本、保留策略和图纸合规风险需由部署方自行评估；关闭 response 存储会限制多轮对话。

## v0.2.0 · 2026-08-29 · 图纸转三维与平台服务

### Added

- 将前端 viewport 从静态 SVG 投影升级为 Three.js WebGL 实体查看器：优先载入后端 GLB，
  支持鼠标旋转/滚轮缩放、等轴/前/俯视相机、剖切和网格/灯光；GLB 暂不可用时显示明确
  标识的参数化网格 fallback，不再把 SVG 当作真实三维实体。
- 新增 `apps/api` FastAPI 服务（`/api/*` 与 `/api/v1/*`），包含图纸上传、参数校验、
  STEP/GLB 生成、artifact 下载和健康检查。
- 新增 CadQuery/OCCT 几何适配器：安装 `.[geometry]` 时走原生 B-Rep STEP；无 OCCT
  时走确定性 faceted fallback，并在响应中公开 `engine`、`warnings`、
  `productionReady=false`。
- 新增证据优先的 OCR 服务：尺寸值、单位、来源视图、置信度、bbox、特征和未决项均
  可追溯；可选 Tesseract provider；未知图纸保持 `needs_review`。
- 登记客户四视图支架验收 fixture `bracket_support_v1`（兼容别名
  `acceptance_bracket`），源图 SHA-256：
  `ea337023af0158438f9cea2482e8e2d6d4052fc04e7e7f4265956824478c4366`。
  fixture 参数为底板 `100×50×10`、上部全宽 `70×50×30`、总高 `40`、U 槽
  `40/R15`（沿 Y 贯穿50）、矩形顶槽长/宽/深 `30/10/10`，以及两处 `Ø20`
  竖向贯穿切孔（中心距 `70 mm`，侧边显示为半圆缺口）。
- 新增 SQLite PDM：项目/文件、不可变版本、内容 SHA-256、乐观 revision、回收/恢复、
  manifest 和审计事件。
- 新增本地账号与 RBAC：PBKDF2 密码哈希、HMAC bearer token、viewer/designer/
  reviewer/manufacturing/admin 角色及账号审计。
- 新增 CAM/NC 服务：刀具和工序 IR、确定性仿真预检查、碰撞/干涉/包络门禁、审核者
  审批、独立制造角色放行和 NC 下载。
- 收紧 CAM RBAC：`reviewer/admin` 才能审批，`manufacturing/admin` 才能放行；CAM
  服务复用账号权限校验，且同一账号不能同时完成审核与放行。
- 新增 `apps/api/scripts/acceptance_check.py`：无外部依赖即可跑 PDM/RBAC/OCR/CAM
  核心验收；安装 API/geometry extra 后自动追加 FastAPI、几何校验和 STEP/GLB 检查。
- 新增服务层单元测试和 FastAPI 集成测试；新增 `README_PLATFORM.md` 平台使用说明。

### Changed

- 修正四视图支架验收配方的几何语义：上部实体为全宽 `70×50×30`（不再把右视图
  `30` 误作上部宽度）；`30` 表示沿 Y 的槽长，矩形顶槽为宽 X=`10`、深=`10`；
  R15 鞍形切口沿 Y 贯穿 `50`；Ø20 圆为 Z 向贯穿切孔/侧边半圆缺口。旧
  `boss*` 参数名保留为兼容别名，feature type 改为 `side_notch_cut_pair` /
  `vertical_through_hole_pair`。
- 产品规格升级为 v0.2.0，记录客户图纸验收尺寸、SHA-256 和六阶段审核流程。
- 前端 API client 增加健康检查、图纸识别、几何生成、认证、PDM、CAM 调用边界；离线
  localStorage 演示仍可独立运行。
- FastAPI 默认使用 `apps/api/data/joyniu.sqlite3` 保存本地 PDM/RBAC，`JOYNIU_DB` 可
  覆盖；`JOYNIU_AUTH_SECRET` 用于 token 签名。

### Verification

- 仓库虚拟环境执行验收脚本：12 项通过、0 项跳过、0 项失败；仅安装核心依赖时仍会
  将 FastAPI/CadQuery 标记为可选跳过，`--strict-optional` 可作为发布门禁。
- 精确客户图片上传命中 fixture，OCR 结果 `status=confirmed`、16 条尺寸证据和 5 个
  结构特征（含矩形顶槽与侧边切孔）；PDM 原图/参数版本关联、CAM 仿真、reviewer →
  manufacturing 放行链路通过。
- 安装依赖后使用 `cd apps/api && python -m pytest` 执行几何/API/平台测试；发布门禁使用
  `python scripts/acceptance_check.py --strict-optional --drawing <file>`。

### Known limitations

- 没有 `engine=cadquery-occt` 和 `productionReady=true` 的 STEP 只能用于审阅，不能
  作为生产 B-Rep 交付；CI 应安装 `[dev,geometry]` 并启用严格验收。
- OCR fixture 是可审计的校准 profile，不是通用视觉模型；非 fixture 图纸需要安装
  Tesseract 并由人工确认，复杂视图/公差/标题栏尚未自动建模。
- CAM `deterministic-precheck` 是流程门禁，不是机床级材料去除、刀具负载或碰撞仿真；
  生成的 NC 是设计草案，仍需组织批准的仿真和 postprocessor。
- PDM、账号和 CAM/NC 快照在配置 `JOYNIU_DB` 后可跨 API 重启恢复；直接几何 artifact
  下载缓存和 OCR 识别 hand-off map 仍是进程内的短期缓存，drawing-to-model 会把审计
  证据与生成物写入 PDM。对象存储、集群队列、组织级多租户 ACL、完整通用拓扑规则、
  机床级材料去除仿真和多人协作计划仍列入 v0.3/v1.0。

## v0.1.0 · 2026-08-29 · 浏览器工作台原型

### Added

- 创建 `JoyNiu-NewCAD` 独立开发分支，固化 CurrentCAD 深度体验证据与产品规格。
- 初始化 React + Vite 单页工作台，实现 AI 参数解析、轴类零件参数编辑、特征树、三维
  SVG 预览、2D 工程图、装配、标准件库、项目管理和导出交互。
- 使用 browser `localStorage` 保存当前项目与操作历史。

### Known limitations

- 几何由浏览器演示内核生成，STEP/DXF 为流程联调样例，不替代服务端 OCCT/CadQuery。
- 当时尚未接入服务端 OCR、账号/PDM、权限和 CAM；这些能力在 v0.2.0 增加了可运行的
  本地服务边界，但生产化仍需按上方限制部署。

## 2026-09-10 · 非支付客户流程验收

- 修复 HTTP 退出会话撤销、响应正文超时、原图/GLB/投影过期链接恢复与认证下载。
- 修复回收站按不同顺序恢复的项目合并、备份异步切号保护、空白重命名、空工作区及不可用文件恢复。
- 装配预检查拒绝使用过期实体包络。
- 已部署 usability-20260910，353 项前端和 138 项非支付后端回归通过；线上和本地 Chrome 的实际操作及限制见 `docs/nonpayment-user-acceptance-2026-09-10.md`。
