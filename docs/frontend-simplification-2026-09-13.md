# 前端信息层级简化验收 · 2026-09-13

用户反馈：功能完成后，页面存在重复导航、过多并列按钮与常驻面板，主操作不突出。

本次实际查看了 [CurrentCAD 创模 AI 首页](https://ai.currentcad.com/)：窄侧栏、中心输入区、按需进入的历史和工具。参照其信息组织方式重做工作台结构，未复制其品牌素材，也未把未登录首页的观察扩大为对其全部编辑器的判断。

## 本次调整

- 单一导航，专业工具和账号菜单折叠，最近项目及未保存状态保留。
- 首页单个描述／附件入口和真实最近项目列表。
- 建模页以对话及模型为主体，参数与检查按需展开。健康状态简化，失败、澄清问题和交付阻断继续可见。
- 二维按工具分组，文字／尺寸／变换仅显示当前设置；特征和工程页面收起次要参数与历史。
- 修复手机输入框聚焦、明确打开参数的跨页动作、首页附件与描述关联、菜单快捷键焦点及放大预览网格冲突。

## 验证

- 最终前端自动化：531 项通过，0 失败。
- 生产构建通过，沿用现有大包体积警告；本次未做代码分包性能专项。
- `git diff --check` 通过。
- 1280×720 实际页面检查：首页、建模、二维图纸、真实特征模型、真实装配和工程图设置；放大预览实际占用 1260×700。
- 390×844 浏览器视口检查：页面无横向溢出；手机对话输入框可见且正确聚焦；特征预览及二维画布位于属性之前。
- 创建本地验收项目后创建基础轴，参数面板立即打开；从基础工程图返回 3D 可直接编辑参数。该轴为明确标注的参数草稿，用于界面检查，不计为真实实体生成验收。
- 本地已有真实 STEP 特征设计与装配能打开并显示；二维已存修订能打开，切换工具保留文字草稿。
- 本次未调用付费 AI，也未重新运行后端几何专项；后端功能与此前发布记录保持原有证据范围。

## 发布

已发布至 [现有站点](http://122.51.168.205/)，并在公网浏览器实际打开首页确认新导航、输入区及脚本版本。

- Web 镜像：`joyniu-cad-web:studio-shell-20260913`
- 完整镜像 ID：`sha256:c171d1b82ba7fc76689b484c674409c572dfde99b3019958c60c56641a4399bc`
- 入口脚本：`/assets/index-CJ647Fby.js`；入口 HTML 及 JS/CSS SHA256 与冻结构建一致。
- Web 单独切换完成，API 与 cad-egress 容器 ID、镜像和启动时间均未变化。
- 公网 14 项检查全部通过：页面、资源、健康、能力及未登录权限；本轮公网校验不包括登录后新 AI 任务。
- 旧 Web 镜像保留，旧哈希静态资源保留，生产 Nginx 配置未改变。
- 冻结前端源码及产物：`/opt/joyniu-candidates/studio-shell-20260913`；本次没有创建新的 Git 提交或推送。

证据：[前端测试](evidence/studio-shell-20260913/frontend-tests.log)、[构建](evidence/studio-shell-20260913/frontend-build.log)、[发布记录](evidence/studio-shell-20260913/web-switch.json)、[公网验证](evidence/studio-shell-20260913/public-verify.json)、[文件校验表](evidence/studio-shell-20260913/manifest.json)。
