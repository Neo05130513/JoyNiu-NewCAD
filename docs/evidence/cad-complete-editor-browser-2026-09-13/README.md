# 多轮廓、曲面和关联标注浏览器验收

2026-09-13，本地 5186、独立 Chromium 测试配置，1440×960 与 390×844。使用专门的合成验收账号，没有修改用户 Chrome 或 CurrentCAD 图档。鼠标实际绘制、点选、拖动和输入尺寸，未向页面状态或 API 注入预制计划。

1. 鼠标画 40×30 外轮廓、圆孔；图上半径 5→7；再画 10×10 独立外轮廓并拉伸 5。保存两个真实实体，体积 `(1300−49π)×5`。
2. 创建偏移平面和螺旋线并保存。创建 3×3 样式曲面，鼠标拖动中央三维控制点，使 Z 从 6 变成 7.337706；提交后原两个实体保留。
3. 上视图真实点选板面两个位置，创建关联 PMI。两点均有服务器生成的语义引用及参数锚点；保存为 r5，导出含模型和标注的 SVG，刷新后能重新拾取面/边。
4. 在手机尺寸下打开和取消 PMI，页面无横向溢出；桌面参考平面与曲线默认隐藏，可按需显示。
5. 双击原草图并在图上将宽度 40→45，完成为 r6。原两实体、独立曲面、孔和标注均保留；两标注点 X 从 31.688498402555922 跟随至 35.64956070287541，Y/Z 不变。再次点击导出 STEP。

两个页面下载 STEP 均用独立 CadQuery/OCCT 进程回读：有效、两个实体。修改前 5730.309799870473 mm³；修改后 6480.309799870463 mm³，与解析体积差小于 1e-7 mm³。几何检查不改变原图/制造确认状态。

`original-browser-records.json` 和 `linked-browser-records.json` 记录页面实际提交响应；页面脚本错误均为 0。`step-readback.json` 含尺寸、实体和 SHA256。参考隐藏等后续界面调整只对新截图复验，早期草图/拖动截图保留各自阶段的真实外观。

可复用入口：`scripts/cad_complete_editor_e2e.cjs`，凭据由临时文件输入，不保存到证据。此处为本地验收，最终公开站点的验证在发布记录中单独标明。
