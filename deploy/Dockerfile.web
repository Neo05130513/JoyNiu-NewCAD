ARG NGINX_IMAGE=nginx:alpine
FROM ${NGINX_IMAGE}

# Build dist locally with VITE_API_BASE=/api/v1 before building this image.
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf
COPY dist/ /usr/share/nginx/html/

EXPOSE 80
