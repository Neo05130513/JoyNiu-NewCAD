# JoyNiu NewCAD 产品规格

## 2026-09-13 二维 CAD 独立编辑界面

- 以用户提供的 CurrentCAD 二维编辑截图为布局依据：独立全屏、紧凑标题栏、六个功能区页签、分组图标工具、黑色画布、十字光标、浮动视图工具、底部命令行及模型/布局状态栏。保留 JoyNiu 彩色品牌；属性面板按需打开。
- 实际连接原生图元操作：命令/坐标绘图、窗口选择、图上移动/复制/旋转/缩放/镜像/端点拉伸、直线相交修剪、两条直线圆角/倒角、图层、标注、约束及原有参数图库。新增原生椭圆、DXF 图元组、带模型视口的纸空间布局，支持持久化和历史回退。
- 输入字母直接进入命令行；支持绝对/相对/极坐标、旋转角度和缩放比例；保留未完成输入保护、请求幂等、账号隔离与失败恢复。
- 验收边界：纸空间当前提供已有布局查看、新建带视口布局及出图设置，模型编辑命令在纸空间禁用。修剪与圆角/倒角当前限直线，拉伸限直线/多段线端点。图元组完整保存在 DXF；既有 DWG 回写核验遇到不支持的 GROUP 等对象仍明确拒绝，不静默丢弃。本次不宣称覆盖竞品全部 CAD 命令。
- 验证记录见 [二维 CAD 界面及交互验收](docs/native-cad-ribbon-2026-09-13.md)。

## 2026-09-12 上线交付收尾（技术发布与合成验收完成）

按用户“那你继续”授权推进最新上线差距清单：先补齐统一交付清单与来源版本，再固定候选代码/镜像、验证旧数据和新工具、完成生产发布验收。保留此前明确排除的多人共编、小程序、密码受限分享；正式业务资料与域名不得凭空填写，收费不因代码更新自动启用。

当前结果：统一交付中心及新版 CAD 功能已发布到现有公网入口，真实 Codex 合成建模、独立 STEP 检查、确认、导出、交付包封存及重开通过。最终前端 514 项、API 全量 2135 项通过；后续部署与工具另有 58 项专项通过。发布镜像、恢复过程、实际耗时和未完成的正式经营条件见 [发布与验收记录](docs/currentcad-release-2026-09-12.md)。

- 新增同账号的“交付中心”：从工程库、手工特征、原生二维及符合既有导出门禁的 AI 结果创建一套不可变文件包；可重开清单、下载单文件及整包。
- 每套记录名称、时间、备注、源 ID/实际版本、可核实的来源关系、文件大小和 SHA-256；在包内保存清单与文件，后续源设计修改不改变历史交付包。包创建不等于客户人工验收或制造放行，状态按源结果如实展示。
- 交付包及原生/工程设计由服务端同账号权限控制，跨账号不可读；重试创建幂等，缺失或篡改文件明确拒绝；不接受客户端路径、临时令牌或自填确认状态绕过门禁。
- 新版 Linux 候选镜像必须包含新 DWG 工具及适配器、几何和 PDF 依赖。用隔离数据库做文件生成/回读、认证路由、旧数据恢复与新目录备份验收；正式发布前固定镜像、备份与回滚路径。
- 真实客户 CAD 验收、正式条款与收费启用、持续异地目标与站外接收人需以真实资料和实际结果完成，不以本地回归或占位配置代替。

> 版本：v0.5.5 · 更新：2026-09-10（商用一期禁收费版本发布记录） · 来源：CurrentCAD 深度体验线程 `01a039b4-773a-7e20-b2f9-143a67025613`、V2-detail 客户核对及使用验收


## 2026-09-12 CurrentCAD 功能补全（本地实现与验收完成）

用户授权以 CurrentCAD 建模成功率 100% 为规划假设，补全 [抖音能力核查](docs/currentcad-douyin-capability-audit-2026-09-12.md) 内除多人实时协作编辑、微信小程序、密码受限分享以外的功能。该假设不是对竞品或本系统实测成功率的声明。此次授权包含自主实现和本地验收，不改变正式交付核验规则。

验收范围：

1. 原生 DWG/DXF 导入、实体选择/捕捉/编辑、绘图与变换、图层及标注样式、撤销重做、保存和实际格式导出；保留不支持编辑的原图实体。
2. 二维几何与尺寸约束、参数和公式、可复用图库，建筑/给排水/电气/暖通/结构绘图构件；输出须是真实可编辑图元。
3. 原生尺寸和公差清单 XLSX、气泡编号与检验图，纸张/比例/颜色 PDF 输出。
4. 可编辑特征属性、草图轮廓、排序/抑制/删除、参数表达式与真实实体重建；新增齿轮、弹簧、旋转、阵列、倒角、抽壳、扫掠、放样。
5. 外部 STEP 导入查看和拓扑测量；多零件实例、变换、基本配合、实体干涉、装配 STEP 与 BOM。
6. 通用实体/装配的投影、剖视、图纸布局及标注输出，保留来源与版本。
7. 照片建模的尺度和已知尺寸输入，模型不确定内容可确认/补充；客户可运行的案例库和分步教学。
8. 保留现有 AI 对话、工程图建模、云项目/版本、账号隔离、核验/确认/交付状态与已实现商用工作；新增功能使用同一账号边界。

按模块验收实际 API、文件回读、必要几何正确性及浏览器用户流程。工程内容不能用包络盒、假下载或按钮占位替代；不可兼容的格式必须明确报错并保留原文件。过程与验收证据见 [补全与验收记录](docs/currentcad-completion-2026-09-12.md)。

后续用户体验验收要求：独立工具切换保留当前账号草稿，导航与保存状态对应实际文档，撤销/重做同步输入框，失败不丢弃上个有效实体，导出 PDF 嵌入中文字体，手机端主要操作可达。真实流程、修复及现有边界见 [用户视角测试记录](docs/user-experience-qa-2026-09-12.md)。

## 当前交付方式与计费规则（2026-09-12）

客户登录网站自助 2D 转 3D。当前由运营线下收款，在后台登记到账充值；线上购买使用独立开关，默认关闭，不因启用积分扣费而打开。AI 服务接入、域名／HTTPS及账号安全方案按用户要求暂缓，本轮保留现有模型、登录和出口。

### 已确认的收费规则

- 固定兑换 **10 元 = 1000 积分**，即 1 元 = 100 积分。线下收款金额精确到分，由服务器计算到账积分；赠送、补偿和调账分开记账，不计为实收收入。
- 模型消费按已核对的官方 API **Standard** 美元基价折算：`积分 = 各类实际 Token × 对应美元单价 / 1,000,000 × 美元兑人民币结算汇率 × 100 × 收费倍率`。一轮汇总后向上取整一次到整数积分，不逐调用取整。基础倍率 1，可在后台调整；初始汇率 7 为可修改的经营结算参数，不冒充实时银行报价。
- 当前核实目录为 `openai-standard-2026-09-12`，支持实际运行的 `gpt-6-astra`。普通输入、缓存读取、缓存写入、输出分别计算；推理已经包含于输出。单次输入超过 272,000 时输入／缓存价格 ×2、输出 ×1.5。基价在服务端维护，浏览器不能覆盖。
- Codex 聚合用量无法证明单个请求长度；不对累计输入盲加长上下文价。缓存写入明细或长上下文依据缺失时，免收无法确认的附加费用，账单注明范围；核心输入／缓存读取／输出缺失则待核查，不估算扣分。此值是客户的 API 等价折算基准，不等同个人订阅的实际现金成本。
- **只有 ready／review_required 的成功候选轮次收费**。failed／needs_input／cancelled／interrupted 不收费；后续继续或重试作为新轮次，只计算新轮次实际调用。
- 任务结束后先扣剩余余额；不足部分记待补缴，充值优先补缴，补足后才能开始新收费任务。没有预估最高消费、冻结、预算上限暂停。
- 改倍率／汇率会保存不可变新版本，并记录管理员和原因；新任务使用新启用版本，已开始的任务保持原版本，历史账单不追溯重算。同一轮次重复结算不重复扣分。

### 线下运营与交接

- 充值单关联客户、实际到账金额、唯一外部收款凭证、登记时间和操作说明；跨操作人重复凭证被拒绝。新客可先入账，首登后本人接受正式条款才可开始收费任务，充值不代表代签条款。
- 客户可查到账与消费记录；财务可按客户、来源、日期和凭证查账。全额线下退款关联原单、实际退款凭证与人工确认，要求可回收积分足够；不自动转账、不抹去原始账目。已消费余额、部分退款和已开发票红冲转人工售后处理。
- 支持工单新增数据删除申请与人工处理回执，区分请求范围、实际删除、保留原因／期限和备份处理，不以关闭工单或回收站代替物理删除。
- 定时运行状态采集与受协调的备份工具，先阻止新请求并验证无活跃工作，再停写备份和恢复服务；占用、失败和陈旧报告可见。异地存储及外部通知需另有真实目的地配置。
- 客户指南、数据处理操作说明、正式条款草稿与 STEP 离线验收工具已补齐。正式条款仍需经营主体、联系方式和保存期限等业务信息，不自动发布占位文本。

软件及部署的具体完成状态、验证证据和外部待办见 [本轮交付记录](docs/commercial-delivery-2026-09-12.md) 与 [交付差距清单](docs/customer-delivery-readiness-2026-09-12.md)。现有模型离线回读不替代真实客户图纸与 NX／SolidWorks／Creo 实机验收。

## 管理后台运营能力增强（2026-09-10）

本次以真实运营流程补齐独立后台，替代“主要提供数据查看”的首版交互。不是添加没有业务逻辑的菜单。

- 导航按运营工作台、交易与积分、平台配置组织；独立订单、流水、结算、退款、发票、模型用量和套餐页面。提供按权限限定的顶部快捷搜索、可收起侧栏与小屏导航。
- 运营总览增加北京时间 7/30/90 日真实趋势及每日明细。任务按创建日期分组并显示当前状态；ready 才计完成，待确认/待补充单列。全部账号、业务客户、后台成员分列；待处理工单仅 open/in_progress，已解决工单不算积压。历史缺失用量保持未知。
- 客户档案可维护企业名称、联系人、电话、标签和内部说明；独立追加跟进记录，保留作者与时间。详情以标签页组织，直接进入该客户的任务、订单、积分流水、工单或账号管理。ops/support/admin 可维护资料与跟进，其他有读取权限的岗位只读。
- 任务增加独立人工处理状态（待跟进/处理中/已处理等以界面定义为准）、优先级和不可改删的内部备注。人工标记不会改变 CAD 真实运行状态或触发重新建模；原有停止接口继续要求权限、原因并等待工作进程确认。提供中文排障建议和白名单诊断导出。
- 财务记录支持服务端客户编号、记录编号、状态/类型、起止日期过滤，过滤后计数和分页一致。新增管理员积分流水入口；日期按北京时间整日转换，混合时区时间戳按实际时间排序。各独立页面只展示相关操作，不反复堆叠费率与套餐；仅导出明确标注的当前页 CSV，防公式注入、不包含凭证或原始支付载荷。
- 客服提供全部待处理、分配给我、未分配队列；支持负责人、优先级、状态和搜索。负责人只选有客服权限的活跃成员。客户沟通与内部备注分开，客户接口不能读取内部备注或内部人员分派；常用回复只填草稿，由操作人确认发送。
- 新增客户资料/人工状态版本号、幂等回执和不可变跟进/内部备注，变更与审计同一事务；版本冲突提醒重新核对，不覆盖另一位同事的变更。请求按账号和记录隔离，Token 刷新不丢当前表单和幂等编号。

继续排除费用预估、冻结积分和预算上限暂停；不启用收费、真实支付、退款、站外消息或自动 AI 重试。数据图表仅使用实际数据库，不生成示例营业额或虚构客户增长。已完成本地浏览器、接口及角色回归并部署，实际范围见 [后台运营增强与验收](docs/admin-operations-v2-2026-09-10.md)。

## 独立管理后台（2026-09-10）

客户工作台保留设计、任务、积分和个人工单。后台以 `/admin` 为独立入口，复用现有登录会话、服务和数据库；无需再次复制客户工程数据。切换后台保留工作区草稿及任务状态，卸载客户视图的全局快捷键和弹窗。

- 运营概览：账号、全部历史任务、当前队列、待处理工单；拥有财务权限时显示净到账及积分汇总。累计指标与趋势口径分别标注，不伪装成实时告警。
- 客户详情：账号基础、近期任务、工单，以及按权限显示的钱包、订单、积分流水和已记录用量。支持客户搜索、账号状态筛选和分页。
- 任务管理：全站搜索、状态和客户过滤、分页、任务详情；兼容新队列注册表和旧 CAD 修订，仅展示每次任务的最新修订。诊断只投影安全结构化字段；历史没有计量记录的用量保持未知。
- 管理停止任务：运营/管理员必须填写原因，重复提交同一操作编号幂等；停止请求与审计同事务，等工作进程实际结束再显示终态，不代客户重新发起收费建模。
- 原有套餐、账单、退款/发票、计费政策、支持工单、服务条款和账号管理迁入后台。客户工单摘要可直接进入受保护的工单详情。
- 系统状态：运行模型/推理配置、内核可用性、数据库连接、任务容量、CAD 存储容量及业务异常快照。外部持续监控、通知、自动异地备份监控尚未接入，页面明确说明。
- 审计：合并账号、账单、工单及运营操作记录；敏感客户/任务详情访问也记录操作人和对象，不输出原图、对话、模型代码、凭证或原始日志。

后台角色分为管理员、运营、财务、客服、只读审计。管理员拥有全权；运营可查看任务和发起停止；财务可查账、核账、售后、调积分；客服可查客户与任务并处理工单；审计可查记录与财务数据但不能修改。套餐价格、计费规则、条款及账号角色仅管理员可改。前端菜单和每个后端操作按同一权限矩阵校验，原工程角色不自动取得后台权限。

本轮沿用既定积分方式，不加入费用预估、任务积分冻结或达到客户预算后暂停确认。实际支付/退款、正式费率、域名 HTTPS 等外部配置的可用性仍以专项验收结果为准。

## 商用一期：在线积分服务（2026-09-10，已部署基础模块，收费关闭）

### 已确认的范围与当前开关

主售能力为机械工程图 2D 转 3D，采用“客户登录网站自行转换、在线购买积分、按实际用量结算”。本节替代此前“不开放自助注册和自助支付”“年度合同额度为主”的规划，也替代本节早期讨论稿的尚未实现状态；后文历史功能规格保留。现有发布版本号不因本轮本地实现而自动升级。

用户明确拒绝的三项设计继续排除：**建模前预计积分或本次最高消费提示、任务积分预占冻结、预算用尽后暂停并要求同意继续**。服务端容量、时长、调用次数限制及启动准入属于运行保护，不增加客户预算确认流程。

本轮已完成下述代码、专项测试和界面接入，并部署到 `122.51.168.205`；**真实支付和退款尚未验收，正式销售未开启**。积分套餐不预置价格；用量费率、收费终态和余额不足策略均未配置，收费默认关闭。管理员必须明确填写并发布规则才能启用，不能把已实现计费代码理解为已经向客户扣费。支付缺少有效配置时购买入口禁用并说明原因；正式条款缺失也禁止购买和收费任务。

### 产品与交付边界

- 定位为工程图建模与核对工具。输入格式、页数、大小和几何复杂度以实际解析限制及已验收样本为准；缺少尺寸或存在矛盾时保留待补充、待核对状态，不宣传任意图纸全自动成功。
- 已有 STEP 实体、可编辑参数、项目及模型版本记录；本轮增加自包含 HTML 核对报告。客户 CAD 原生格式、完整特征树及统一交付 ZIP 不在本轮新增交付承诺内。
- 正式报告默认要求当前模型已确认、实体就绪且结果未过期，无未保存参数变更、阻断问题或执行中的新任务。报告列出中文参数名、单位、名义值与派生值、来源图和文件指纹、已测与未测项目、未决问题及实际确认人和时间；缺失的历史字段明确标注未记录。候选报告必须标明候选，不能冒充确认交付。
- 报告只引用记录中的测量证据，不自行计算公式冒充实测，不包含身份凭据、图纸访问令牌或外部资源。模型检查和客户确认均不构成制造保证，生产使用仍需客户完成工程核对。
- 首版以独立账号和钱包服务客户。企业共享钱包、客户间转赠、交易、提现、订阅、返利及复杂优惠未纳入本轮；现金退款通过订单退款流程处理。

### 逐模块实际状态

| 模块 | 本轮已实现 | 外部条件与当前限制 |
| --- | --- | --- |
| 账号与运营用户管理 | 自助注册开关、登录、改密、会话刷新与撤销、登录限流；管理员真实用户列表、角色、停用和重置密码，保护最后一个启用管理员 | 自助注册默认关闭，由运营启用；邮箱验证和邮件找回未接入，不显示为可用；不提供共享默认管理员口令 |
| 会话与账号切换 | HTTPS 持久会话使用 HttpOnly 刷新 cookie、轮换及 CSRF 校验；非持久会话到期清除登录状态并保留本账号草稿；切号、退出及令牌更新防止旧异步结果写入新账号 | 正式域名、HTTPS 和固定认证密钥仍属部署配置；不把 HTTP 临时登录描述为已具备生产持久会话 |
| 账号云工作区 | 服务端按已鉴权用户保存完整项目、聊天、参数、文件与模型版本快照；修订号并发校验，账号和 API 地址隔离缓存，保存失败和冲突可见，保留本地草稿 | 公共旧浏览器项目只在客户明确确认归属后导入，每账号一次；资格和待导入状态跨刷新保留，不静默导入、串号回退或覆盖云端；大文件实体仍由 CAD 文件服务保存 |
| 任务中心与容量 | 本人任务分页、状态筛选、队列、全局及单账号并发限制、请求幂等、断线恢复、真实取消和实际用量记录；刷新和重复点击不重复创建已接收的任务 | 上游 HTTP 已在执行时需等待有界请求结束，期间仍为取消请求中；不把停止前端轮询当作后端已停止；生产容量和负载范围尚待验收 |
| 调用计量与结算 | 真实供应商调用独立编号、执行尝试关联、费率和条款快照、终态清单封存、待核账及幂等恢复结算；客户与管理员可查实际结算记录 | 仅按有证据的用量结算，缺失用量或规则不猜测；未接入计量的旧通用 AI 入口在商业开启时由服务端关闭，不能通过旧入口绕过收费 |
| 钱包与公开费率 | 事务账本、充值、实际扣分、补偿、待补缴与退款记录；套餐、购买、订单、流水和费率展示；规则版本管理、启停及修订冲突保护 | 当前套餐价格和收费规则为空，收费关闭；两种余额不足实现均待用户明确选择，无默认费率、收费终态或不足策略 |
| 微信支付与现金退款 | 微信 Native 实际请求适配器、响应与通知验签、解密、金额及商户校验、幂等入账、主动查单；整单原路退款提交、查询及通知处理 | 尚未配置并验证真实商户付款、到账和退款；支付宝未接入；真实退款执行默认关闭，部分退款未开放；没有宣称自动每日渠道对账已经运行 |
| 运营账务与申请 | 套餐新增和上下架、真实订单/流水/结算及成本记录、带原因的管理员调账；退款和发票申请、审核、退款处理状态 | 未知上游成本显示待核实，不生成虚构毛利；发票申请不代表自动开票，尚无税务开票系统接入 |
| 正式条款 | 服务、隐私、积分三类文本的管理员发布、版本记录、客户主动接受及订单/任务收据；缺少当前文本或接受记录时阻止相应收费操作 | 三类正式正文目前均为空，待运营提供并审阅后发布；程序和测试不替代正式条款或模型服务授权 |
| 图纸访问 | 本人任务归属校验、约一小时有效且绑定文件版本的签名访问、本人撤销及审计、停用账号拒绝；旧永久令牌默认禁用且商业模式强制禁用 | 本轮未完成完整外部安全审计；客户数据处理地区、保存和删除政策仍需结合正式服务方案确定 |
| 支持工单 | 真实数据库问题单号；客户新建、查看、追加说明和关闭；管理员回复及状态流转；分页、并发版本、幂等防重和动作审计 | 可选关联任务必须归属本人。图纸授权默认否且可撤回，只保存授权记录，不给管理员开放 CAD 图纸下载或绕过现有鉴权；无邮件、短信或站外通知，客户在此查看回复，不承诺处理时效 |
| 备份与发布 | 完整数据目录与 SQLite WAL 一致性备份、清单校验、隔离新目录恢复工具及本地演练；保留旧镜像与数据的发布/回滚说明 | 自动异地备份、监控告警未开启；本轮已完成生产完整备份、隔离恢复迁移和基础线上验证，并留存一份本机备份；真实支付仍待验收 |

### 按实际用量结算的已实现合同

客户流程为登录 → 查看公开规则并接受正式条款 → 在支付开通后购买积分 → 上传校验 → 直接开始任务 → 排队、处理或补充 → 查看实际结算 → 核对并确认导出。当前收费关闭时不展示虚构费率，也不把积分购买作为已可使用的真实收款能力。

1. 结算单位是一次执行尝试（`attempt`），结算时机固定为其终态（`attempt_terminal`），不是每次子调用分别扣分，也不是下载或确认时扣分。同一业务任务的补充、重试及尺寸定位可以形成独立尝试，各自记录实际调用，已结算尝试不会重复扣款。
2. 真实调用使用唯一 `callId`。主规划、原图转录、空间解释、独立复核、提问前复读及来源尺寸定位在供应商边界记用量；供应商外层重试单独记调用。Codex 一次 CLI 执行使用该次汇总用量，不递归累加继承的 trace。命中的既有定位缓存不产生新的模型调用费用。
3. 普通输入为总输入扣除其中的缓存输入；缓存输入单独按对应费率计算。推理 token 已包含在输出时不再加计。费率按供应商和明确模型配置每百万 token 的普通输入、缓存输入及输出积分，不使用模糊模型通配或虚构换算。积分使用整数，全部调用汇总后每次尝试只向上取整一次（`ceil_once_per_attempt`）。
4. 每次尝试固定费率版本和适用条款收据，结束时封存预期调用清单。缺 usage、调用落账未齐、上游结果不明或缺少匹配费率时保持待核账，不把缺失值写成零，不猜测扣款。后台定期及任务完成/读取时恢复用量与结算，账本幂等保证重入不重复扣分。
5. 哪些终态收费由运营明确配置，可选就绪、待核对、待补充、失败、已取消和已中断。**当前收费终态列表为空**，不能从技术状态自动推导“失败一定免费”或“失败一定收费”。失败、取消、补充和质量申诉的对客规则须在启用前写明并发布。
6. 充值款、实际用量积分、积分补偿与现金退款分开留账。重复确认、浏览、旋转或下载已有产物不重复结算；真实取消先停止后续调度并确认在途调用状态，再按已发布规则处理实际用量。

### 余额不足：两种实现待选择

两种策略均已实现并有专项测试，**尚未选择任何一种**。管理端必须明确选择策略、供应商/模型费率和收费终态，保存有效版本并明确启用，不能用空白字段隐含授权。

| 策略 | 已实现行为 | 客户提示与边界 |
| --- | --- | --- |
| 待补缴（`deferred_due`） | 本次先扣已有余额，差额独立记为待补缴；后续充值、正向调账等入账优先清偿待补缴，过程原子留账 | 有待补缴时禁止开启新的收费任务；钱包展示待补缴与充值优先抵扣说明，不能把充值全额表述为可用余额 |
| 余额封顶（`cap_at_balance`） | 本次最多扣至零，差额由平台承担并单独审计 | 不形成客户负余额或追加收费；平台承担金额与本次实际扣分分别展示 |

收费任务启动检查账户、余额、待补缴和条款状态；排队后进入真实供应商调用前再次检查适用准入。上述均不预占积分，也不在运行中按客户预算暂停。积分包价格、兑换比例、赠送与有效期政策、终态收费范围及不足策略仍待业务确认。

### 支付、退款与正式条款边界

订单由服务端使用当时套餐、金额和积分规则创建快照，客户端不能指定实际成交金额或改钱包。微信支付成功须由验签且核对无误的服务端通知或主动查单确认，重复通知只到账一次；客户扫码界面和前端提示不作为入账证据。

现金退款当前仅支持经审核的整单退款，真实渠道执行需单独开启。提交前足额回收该订单对应积分，渠道结果不明保持处理中并允许查证，只有明确成功才标记已退款；明确失败按账本流程处理回退。申请通过不等于资金已退，不能将积分补偿当作人民币退款。已消费、存在待补缴或无法足额回收时的业务政策需在正式规则中明确，不能用本轮实现代替客户退款承诺。

正式服务、隐私及积分条款的文本和版本发布能力已具备，但正文尚未提供。运营仍须准备经营主体、收款商户、发票和支持安排，明确价格、有效期、消费及不足策略、取消/失败/质量申诉、退款、数据处理和服务终止后的余额处理。客户接受选项不预勾选，保存接受的实际版本；未发布正式文本时不开启销售。

### 模型授权、数据与上线前未完成事项

正式商用模型服务授权**尚未核实**，现有个人登录或代理调用可运行不代表获准向客户提供付费服务。本轮没有切换现有模型通道，也不把更换密钥或服务器地区当作授权已经解决。供客前需核实供应商允许的集成用途、客户地区、数据处理安排和合同条件，并用获得授权的真实样本验收实际通道。

客户原图来源权利、保密要求、供应商和处理地区、保存期限、删除方式、人工访问权限仍须形成实际可履行的告知与约定。正式域名与 HTTPS、主体及适用备案/许可、开源依赖许可清单、正式条款和模型授权须由运营按实际经营场景核查；本文不代替相应审阅或认定。

上线前仍需完成：真实商户小额支付与退款闭环、故障和遗漏通知对账、正式规则及文本发布、样本质量与容量验收、自动异地备份和告警配置。本轮已完成生产完整备份、隔离恢复和禁收费版本发布；具体证据见 `docs/commercial-acceptance-2026-09-10.md`。备份工具本身不会自动停止生产或覆盖恢复目录。

### 客户界面与本轮验收

一期客户流程围绕 2D 转 3D、我的项目、转换任务、积分与订单、帮助和支持工单组织，账号安全与运营管理按权限进入。工作区继续提供完整 3D、对话及独立图纸核对；已补充云保存、任务、积分、退款和当前支付不可用状态的使用说明。装配、CAM、标准件和通用 2D 等历史功能及数据保留，不把它们扩展为本轮新增销售承诺。

已完成各模块专项自动化测试、前端构建及本地界面检查，覆盖账号隔离、迟到响应、云冲突与旧数据显式导入、幂等账本、真实取消、规则版本、不足策略、退款状态、工单权限和防重等。本轮这些验证没有调用真实付费模型或完成真实商户付款；不能据此宣称生产部署、全量安全审计、长期运行容量或自动异地恢复已通过。部署操作与待验收项见 [商用发布与备份说明](deploy/commercial_release.md)。

实现主要位于 `CustomerShell.jsx`、`accountWorkspace.js`、`workspaceSync.js`、`TaskWorkspace.jsx`、`BillingWorkspace.jsx`、`AdminBillingPolicyWorkspace.jsx`、`AdminUsersWorkspace.jsx`、`CommercialTermsWorkspace.jsx`、`SupportWorkspace.jsx` 及 `cadDeliveryReport.js`；后端为账号/工作区、CAD 任务计量、账本/费率、微信适配、条款和工单各独立服务及路由。规格记录的是当前已实现能力及其未配置边界，不补填用户尚未决定的商业规则。

## v0.5.5 我的项目浏览改版（2026-09-09）

- “我的项目”默认显示全部项目的文件卡片；左侧常驻项目列表，一次点击即可筛选，不再通过下拉框选项目。项目管理页隐藏全局侧栏重复的项目清单，其他工作台仍保留快速切换。
- 支持跨项目搜索文件名称、类型与所属项目，按零件、工程图、装配体、文档筛选，按修改、打开时间或名称排序。点击文件名称或打开按钮直接进入对应工作台；卡片展示所属项目、类型、修改时间、当前打开状态和版本。
- 下载、重命名、复制、保存版本、历史与删除收纳进文件更多操作。跨项目操作显式携带所属项目，保留当前草稿；同项目搜索中改名或自定义副本名后仍可看到目标文件。保留备份导入导出、文档编辑与回收站恢复。
- 从项目页新建项目后仍停留项目管理；点击项目只浏览文件，不自动展开上次使用的文档。旧版缺少内容的记录禁用打开，并保留新建同类型文件入口。
- 桌面项目列表可独立滚动，小屏采用双列项目入口和单列文件卡片。超长名称省略并保留完整标题，避免撑开列表或操作按钮。

## v0.5.4 客户日常使用完善（2026-09-09）

- 项目与文件可复制、删除及从回收站恢复；副本独立编辑并建立自己的版本记录。跨项目切换记住最后打开的文件。历史恢复继续自动保留恢复前草稿。
- 项目页和设置页提供备份导入，支持当前格式、旧工作区备份和单文件 JSON，追加导入并重新生成本地标识，保留已有项目。导入前校验格式、20 MB 大小限制及数据结构，错误不得破坏当前工作区。备份含草稿、文档和版本；原图及实体文件依赖原 CAD 服务。
- 快捷命令支持中文搜索、方向键选择、回车执行和 Esc 关闭。弹窗内部空白区域不会误关闭；导出菜单支持外点、Esc 和选取后关闭。手动创建草稿使用“创建草稿 / 设置尺寸”流程，错误参数引导修正，避免误导客户重新上传。
- 三维预览可重复点击相同视角复位，滚轮与按钮统一按 10%–1000% 显示和限制；读取 GLB 失败可直接重载。实体投影支持放大、适应、原图打开、下载及失败重试，下载超时或离页后停止等待。
- 标准件搜索支持常见的 M8x30 写法；旋转件及螺钉头按实际包络留置间距。当前通用实体的有效包络纳入装配预检查，尺寸已失效时明确说明。2D 图层全部隐藏时提供恢复入口并阻止空 DXF 导出。
- PDM 可搜索、分页查看全部文档、展开历史版本并下载。退出账号、切文件或更换模型后，旧异步结果不能覆盖新工作区；只读成员缺少 CAM 权限时仍可查看已授权文档。CAM 按角色、模型和流程状态给出可操作范围，角色名称使用中文。

验收记录见 [前端客户使用验收](docs/frontend-usability-audit-2026-09-09.md)。本轮跳过上传图纸生成 3D，重点验证日常编辑、保存恢复、查看导出及错误交互。

## v0.5.3 原图与参数核对（2026-09-09）

客户需要在生成模型后查看上传原图，并逐项核对参数。根据客户试用反馈，原图核对独立成页，为图纸和参数留出足够空间。

- 建模页中间保持原有完整 3D 预览；有来源图纸时，通过“图纸核对”按钮进入独立页面。核对页左侧大幅显示原图，右侧显示可搜索的参数列表，提供“返回 3D 建模”入口。核对页不占用建模页的画布空间，切换页面保留当前参数、模型和对话。
- 核对页支持文件选择、PDF 分页、缩放、平移和适应图纸。悬停参数或以键盘聚焦时，用箭头指向具体尺寸文字的小框，并显示原图中该文字的局部放大；主图保留整张图纸便于判断位置。点击参数或图上文字框可固定核对，也可主动放大定位。显示图纸原文与当前值，核对列表只读，修改参数时返回建模页。建模页参数悬停不展开图纸、不改变 3D 布局。
- 参数通过独立读图标注的稳定 ID 关联原图，支持一个派生参数关联多个标注。新读图结果记录归一化文字框，严格按来源图和坐标系转换；显示框仍为 AI 候选位置，供客户核对。
- 旧项目打开核对页时，自动根据已保存原图及标注的 ID、原文、区域、尺寸基准补充文字位置，结果单独缓存，保留其原图、转录、模型和修订。不同位置的同值尺寸必须分别定位；派生参数逐项列出依据，并明确提示尚未定位的依据数量。只有参考区域时不画大范围高亮冒充尺寸位置；识别失败或无法区分的标注显示“尚未定位”并可重试，不按相同数值猜测关联。
- 定位期间展示已检查/总标注数量、已定位数量和用时，每个区域完成后立即显示已有尺寸。页面重开时接续正在处理的任务，连接中断时读取后台结果；服务端排队、准备和识别合计最多 210 秒，页面最多等待 225 秒后结束加载并允许重试。手动重试按操作 ID 区分新旧结果，切换模型或离开页面停止等待，迟到响应不得覆盖当前模型。
- 建模参数面板、原图核对列表及关联公式使用易懂的中文尺寸名称，新模型为每项参数生成中文显示名；旧模型按已有中文名称和通用工程词汇显示，无需重新建模。名称包含必要的部位、方向或基准，例如“顶部至孔中心距离”；保留内部变量键、计算公式、原图引用和参数数值，支持中文名称及原变量名搜索。角度按角度单位显示，不由全局长度单位覆盖。
- 服务端按运行与修订提供原图下载及预览，沿用访问权限和下载令牌。刷新、重开项目及查看历史版本时仍使用该版本原图；加载旧项目的图纸元数据不得覆盖其模型、参数或交付状态。
- PDF 页坐标从读图时的拼接图映射，未进入读图拼接图的页只供查看。DWG/DXF 提供已有解析器的布局预览，无法证明与读图坐标同源时不高亮位置；预览失败可以重试或查看原文件。

验收：验证文件身份不符时无错误高亮、多页坐标转换、多个来源引用、重复数值和直径/半径前缀的区别、文字框与原图局部一致、部分依据定位状态、参数改值后保留原图来源、悬停/固定/缩放联动及刷新缓存恢复；已有建模与确认检查保持通过。

变更记录：新增参数来源联动、持久预览接口和尺寸文字框采集；根据试用反馈，将同屏对照调整为独立核对页，恢复建模页完整 3D 预览；进一步补充旧图尺寸定位、箭头指示及尺寸原文局部放大。

## 通用 CAD 建模与检查循环（2026-09-09）

- 新上传图纸、空白文字建模及通用模型后续修改，默认使用可调用工具的建模代理；已有专用配方项目保留兼容编辑。通用流程不得引用图纸文件名、哈希或专用样本参数作为正确答案。
- CAD 引擎可通过 `JOYNIU_CAD_PROVIDER=codex` 选择本机 Codex CLI，默认模型 `gpt-6-astra` 可配置；读取原图、空间候选、计划和独立复核使用同一已选择引擎。界面显示当前配置和本轮实际引擎，历史项目不得覆盖。换引擎使旧转录与尺寸依据失效；认证由CLI管理、可执行文件可用不代表认证在线。
- AI 可以查看原图、裁剪/旋转局部、记录尺寸与结构依据、提交通用特征计划、查看实际执行错误和实体投影、修正计划。支持的操作范围以服务端计划 schema 为准；超出能力时必须说明，不使用相似模板顶替。
- 图纸先通过独立视觉转录：该请求不携带 CAD 计划或既有参数，只记录原文、界线/中心线基准、位置及疑问。转录始终是待核对候选，在 UI 中可展开查看；来源哈希完全匹配才复用，读图失败不得继续填默认尺寸建模。
- 读图器 v2 按墨迹和留白将每张栅格图划分为最多四个内容区域，没有可靠分区时保留整图；区域不得冒充已识别视图。最多四个区域请求并行，共享 180 秒，计入默认 900 秒、最多 20 轮操作的完整请求预算。单区瞬态错误仅重试一次，不重读成功区域；认证、权限、额度、上下文等永久错误不重试。任一区域最终失败时整图仍失败，各区域的候选与尝试记录仅供复盘。
- 转录复用必须匹配全部原图和预处理图的 SHA-256、文件顺序及读图器版本。来源变化时清除旧尺寸依据、旧计划和已检查局部；读图器版本变化时重读并清除旧依据。规划上下文保留全部标注及基准，去除重复哈希和传输诊断。
- 数值、关联表达式、尺寸来源、未决问题和特征 ID 必须随计划保存。缺少必要尺寸时保持空值并提问，不得由预览默认值补齐。
- 复杂计划允许使用 `edit_plan` 小批增补，每批最多 12 个特征，校验引用关系后原子保存；失败批次不能覆盖已有草稿。每次增量保存或恢复草稿后，在原隔离工作进程中进行最多 15 秒的实际 OCCT 构造检查，返回失败特征、有效性、包络、实体数及三视图。草稿视图明确标为局部构造，只绑定当前计划；不产生 STEP/GLB，不作图纸一致或交付声明。
- 独立空间分析以原图为唯一输入，记录共同坐标系、基准、材料/开口及跨视图关系，与建模并行。候选不是答案；失败时可继续核对原图，结束后晚到结果不能改写任务。来源变化必须使旧空间候选失效。
- 三点圆弧允许携带指定半径约束，执行器按实际三点圆计算并拒绝半径不符或共线退化，不能静默调整源尺寸来适配构造。
- 实际实体投影须与原图进行像素配准，可靠残差返回具体区域和叠图并触发几何修正。无法排除视向、镜像或旋转差异时只能报告不确定。轮廓支持只是单向证据，不能覆盖缺失特征或替代独立图纸复核。
- 对原图复刻，已知可靠轮廓差异必须阻止确认及 STEP 下载；设计修改只能由服务端已验证父版本与完整用户修改要求建立依据。客户端、模型输出、恢复快照不得自行放宽比较规则。
- 在建模之前独立记录可测要求；整体包络、圆柱面位置/直径、实体数与射线材料区间分别核对。整体包络不能代表局部厚度、孔中心距、半径或孔中心高。任何不一致均反馈给代理，不能仅修改验收期望迎合错误模型。
- `ray_intervals` 证明完整实体截面。服务端保留原 `probe/expected`，根据实际实体包络取得完整材料区间；提交窗口仅作诊断，缩短窗口不能隐藏额外厚度或盲孔底。缺少完整覆盖证据或测量位置不匹配时不得通过，旧检查结果须重新执行。
- 从实际 OCCT 实体输出 GLB、STEP 和正交投影。通用模型禁止模板 fallback；2D 页面显示真实实体三视图。几何有效、尺寸检查通过、AI 图纸视觉判断、用户确认必须分开记录与展示。
- 修改参数后清除旧实体的当前状态，先通过代理重新执行与检查，再由用户确认当前修订并开放 STEP。旧修订、旧模型或尚有尺寸差异的模型不能通过确认入口直接交付。
- 服务端保存原图、每轮提交的计划、工具结果和不可变修订；同一项目文件刷新后可继续编辑。恢复历史版本时保留其内容，通过新建模运行继续，不能用最新服务端数据静默覆盖历史设计。
- 任务在发送 `started` 前持久化 `runId`；浏览器断线后后台继续，GET 可恢复进度与终态，实际服务重启把遗留运行标为 `interrupted`。前端重试使用已存来源，确认与修改互斥并核对 `runId/revision`，换图前自动保存旧版本。
- 只从最终 assistant phase 读取 CAD 动作，不能将 commentary 当作动作；同一最终消息包含多个 JSON 时拒绝执行，不能猜选其中一个。
- 图纸任务的 API 确认与存量 `ready` 的 STEP 下载，必须校验服务端独立复核的来源、当前计划哈希、一致状态、非空结构化 observations 及空 differences/questions/errorCode，并重核真实尺寸证据。旧自评与旧下载令牌不得绕过。纯文字 `not_applicable` 保持可用。独立视觉本身可能漏检，不能等同于准确性或制造批准；页面应明确未测特征仍需核对。
- 通用执行器只接收受限 JSON 指令与算术表达式，在独立工作进程中执行，并设置时间和资源上限。AI 超时、接口失败或建模失败不创建替代实体。
- 每次上游尝试记录安全错误类别、允许的错误代码、HTTP 状态、首个可读数据块/首段输出耗时、事件与字符计数、用量及终态；不得记录供应商正文、推理文本或凭证。总超时发生时保存最后快照，晚到回调不能改写已结束的结果。
- 验收必须分别报告确定性几何测试、真实 AI 识图结果和浏览器端流程结果；使用给定尺寸构建成功不等同于独立读图通过。测试需覆盖未参与实现的结构/尺寸变化、对话修改、参数编辑、确认与重开项目。

最新 Codex 引擎原图盲测已完成：支架 `10.jpg` 的24/24项、轴类 `3.jpg` 的42/42项独立STEP检查通过，与保留参考体的布尔对称差均为0；完整任务分别433.672秒、536.515秒，均正常进入待用户确认。原图是唯一图纸输入，没有向建模流程提供审计答案。这是两个样本的实体验收，不能替代更多未知图纸评估。

早期中转站原图盲测未通过：`10.jpg` 后续实际 STEP 为21/24项，但中央桥缺失，形体对称差25.44%；`3.jpg` 仍出现尺寸端点和孔槽误读，未生成 STEP。独立视觉也可能漏检，不能把候选数量、部分量测或模型自评当作完整复刻成功。各次实跑、外部审计和历史结果见[续修验收记录](docs/cad-agent-autonomy-2026-09-09.md)。

最终完整回归为后端1142项、前端153项通过，生产构建通过。这些检查覆盖实现和交付边界，不替代复杂原图的实际验收。

## v0.5.2 DWG 原生矢量解析与可追溯量测（本次迭代）

### 目标与优先级

- DWG 属于本迭代 P0 输入格式。系统必须先用部署侧的 DWG 原生解析器/转换器读取文件版本、模型空间、图纸空间、布局、图层、块、插入变换、尺寸实体和基础几何，再生成标准化矢量中间件与高清图面；禁止把 DWG 二进制直接当作远程多模态模型可见文件。
- 推荐能力链为“受支持的 DWG SDK/转换器 → 规范化 DXF/SVG/PDF/PNG + 图元清单 → 尺寸与几何量测 → 受限结构化摘要 + 高清渲染交给远程 AI → 人工确认 → 白名单 CAD 配方/特征树”。具体部署可使用经许可的 ODA Drawings SDK/File Converter，或经过兼容性验证的 LibreDWG 等实现，但产品契约不依赖单一供应商。
- 本节自 v0.5.2 起覆盖 v0.2.1、v0.4.0 和 v0.5.0 中“DWG 以 `input_file` 直接转发给远程模型”的旧约定；旧描述仅作为历史记录保留。

### 解析、单位与坐标契约

- 解析器必须识别 DWG 版本，并统一处理 model space、paper space/layout、viewport 比例、`INSUNITS`、标注样式比例、UCS/WCS、块基点及嵌套 `INSERT` 变换。对单位缺失、布局冲突、非 1:1 视口或块变换不确定的情况，不得静默按毫米解释。
- 原生 `DIMENSION` 实体优先读取其实际 measurement、关联定义点、标注覆盖文字、公差、单位、比例、图层和 entity handle。即使显示文字模糊，只要 measurement 与关联关系可由原生实体确定，仍可形成可确认候选。
- 没有可用 `DIMENSION` measurement 时，可以从 `LINE/LWPOLYLINE/ARC/CIRCLE/ELLIPSE/SPLINE/INSERT` 等矢量图元的端点、圆心、半径、包络和投影关系量测长度、直径、半径、夹角或中心距。几何推导必须记录所用图元、变换、公式、坐标系、单位和误差，不得只输出一个无来源的数值。
- 栅格 OCR 仅可读取渲染后的文字证据，不能替代 DWG 原生 measurement 或矢量几何量测；爆炸标注、断裂图元、代理对象和未解析的自定义实体必须明确降级为待确认。

### 字段来源与证据模型

每个候选字段的当前值必须有且只有一个明确的主来源，并在参数面板逐项展示；历史证据可以并列保留，但主来源不得在合并或 AI 对话过程中被暗改：

| 来源 | 含义 | 最低证据要求 |
| --- | --- | --- |
| `direct_dimension` | 从原生 `DIMENSION` measurement 或等价关联标注直接读取 | 文件 SHA-256、布局/视口、图层、entity handle/type、原始 measurement、单位与全部比例/变换 |
| `vector_derived` | 从矢量图元及其几何关系确定性量测或计算 | 参与图元 handles、定义点/圆心/半径、WCS 坐标、单位、公式/量测方法、容差与推导结果 |
| `ai_interpreted` | 远程 AI 基于高清渲染和受限摘要解释视图对应、拓扑语义或字段映射 | 渲染页/区域、模型与请求轮次、AI 原始候选、置信/分歧状态；不得伪装成原生读值 |
| `manual` | 客户或设计师修改、补全并确认 | 修改人、时间、修改前后值、单位与确认记录 |

- 证据对象至少包含 `sourceType`、`fileSha256`、`layout`、`layer`、`entityHandles`、`coordinateSystem`、`unit`、`scaleOrTransform`、`rawValue`、`normalizedValue`、`methodOrFormula` 和 `needsReview`。不适用字段显式为空，不得伪造 handle 或坐标。
- 同一字段存在冲突时不按“更大置信度”自动覆盖：界面并列展示直接标注、矢量量测、AI 解释和人工值。`vector_derived` 以及任何冲突值必须经人工确认后，才能进入生成快照。
- 本地原生解析和量测结果属于 `direct_dimension`/`vector_derived`，不属于 AI 输出。远程 AI 只接收高清渲染图与字段白名单内的受限结构化摘要；服务端不得把本地数值写入模型回答、`ai_interpreted` 证据或“AI 已识别”计数。

### AI、三维拓扑与生产边界

- 远程 AI 负责视图配准、零件语义、特征/配方建议、冲突解释与未决问题；确定性 DWG 解码、实体枚举和坐标量测在本地隔离进程完成。发给 AI 的结构化摘要只包含白名单字段、有限数量的图元/尺寸证据及非执行性文本，不包含宏、脚本、OLE 内容、任意文件路径或可扩展指令。
- 二维 DWG 即使拥有完整尺寸，也不等于自动拥有唯一三维拓扑。只有零件类型能映射到已审核白名单配方/特征树，并且必要尺寸、视图对应和特征关系经人工确认后，才允许生成可编辑 B-Rep。未知拓扑必须先建立/选择配方或由人工补建特征树，不能仅凭外形包络“猜”成生产实体。
- DWG 候选确认、OCCT 实体生成、STEP 回读、PDM 版本与 CAM/NC 放行仍是独立门禁；二维量测成功不得被展示为“三维已正确生成”。

### `1(1).dwg` 已审核配方

- 该验收图明确包含两个同轴回转零件，而不是一个矩形拉伸体：左侧轴向全剖是主件，右侧轴向全剖是独立 M12 镶件。配方标识固定为 `partType=stepped_tapered_nozzle`、`recipeId=stepped_tapered_nozzle_with_insert_v1`。
- 主件轴向轮廓为：总长 98；0–50 mm 浅锥段外径从 Ø54.25449350717895 过渡至 Ø56；50–70 mm 为 Ø30 颈段；70–98 mm 为 Ø25 末段。左端 Ø40 沉孔深 40，之后为 Ø13 贯通孔，右端扩至 Ø17。
- 图面“7.5”对应右端 Ø13→Ø17 扩口的轴向长度；DWG `DIMENSION` measurement 为 7.464101615137906 mm（按标注样式显示为一位小数），不能误写成 7.5°。由轮廓端点和半径差反算的扩口半角为 15°，其来源标记为 `vector_derived`。
- 镶件外径 Ø39.4、长度 40；图纸没有编码相对主件的装配轴向位置，因此 `insertAxialOffset=0` 只能由远程模型作为 `ai_interpreted` 装配基准候选提出并等待人工确认，不能冒充原生尺寸。确认该候选后，与 Ø40 沉孔的派生径向间隙为 0.3 mm；镶件和主件在 STEP/GLB 中保持两个未融合组件。
- `M12` 是标注覆盖文字，图纸未提供螺距、公差等级与固定方式。当前配方只生成名义 Ø12 直孔作为非制造级表示，并必须在参数面板、检查结果和导出警告中说明“无真实螺纹牙型”。

### 失败状态、安全与资源限制

- 转换器缺失、当前 DWG 版本不受支持、文件损坏/加密、必需外部引用缺失、布局不可用、单位不确定、转换超时、文件超限和图元超限必须分别返回可操作的状态，例如 `converter_unavailable`、`unsupported_dwg_version`、`corrupt_or_encrypted_dwg`、`external_reference_missing`、`unit_ambiguous`、`conversion_timeout`、`file_too_large`、`entity_limit_exceeded`。不得统一显示“远程大模型未形成候选”。
- 解析/转换在隔离的临时目录和受限子进程执行，设置 CPU、内存、墙钟时间、页数/布局数、嵌套块深度、图元数与渲染像素上限；结束后清理临时产物。生产默认仍执行单件 20 MiB 上传上限，管理员可以按部署容量显式调整，但前端和 API 必须显示实际限制。
- 禁止执行 DWG 中的宏、AutoLISP、VBA、OLE、嵌入可执行内容或外部命令；不自动访问网络、绝对路径或上传范围外的 XREF/字体/图片。允许的伴随文件必须作为同一次上传显式提供、通过类型和大小校验，并以安全文件名在隔离目录解析。
- 超时或转换失败时保留原文件哈希与安全诊断，允许安装/切换转换器后重试；不得产生空候选、使用模板补齐，或把转换失败误报成中转站/模型失败。

### 端到端验收标准

1. 用户文件 `1(1).dwg`（AC1021，AutoCAD 2007/2008/2009，114,016 bytes，SHA-256 `5c3300b60c9e7c5bfe6dc839c525dd9b9fc4d24175388af38c09b698880b2944`）上传后，系统先报告 DWG 版本、可用空间/布局、单位判定和转换器名称/版本，再展示高清渲染；供应商请求中不得包含原始 DWG 二进制。
2. 对该文件中的原生尺寸，候选能追溯到具体 `DIMENSION` handle、measurement、布局、单位和变换，并标记 `direct_dimension`；模糊或遮挡的显示文字不影响可验证 measurement 的读取。
3. 至少选择一项没有可靠文字但可由矢量确定的距离/直径进行量测，候选标记 `vector_derived`，界面能展开参与图元、WCS 端点/圆心、公式、单位和容差；未经人工确认时生成门禁保持关闭。
4. 远程 AI 仅收到至少一张可读的高清布局渲染和受限结构化证据摘要；AI 返回的拓扑/字段解释标记 `ai_interpreted`，本地解析值不会出现在 AI 来源计数中，也不会被包装成 AI 原始读值。
5. 人工确认后字段来源改为或附加 `manual` 审计记录，确认载荷保留此前直接标注/矢量/AI 证据；修改数值不会删除原证据。
6. 当文件能解析但无法映射到已有三维配方时，界面仍展示二维视图、尺寸表和可量测矢量，同时明确提示“需要选择/建立特征树”；不得生成任意默认 3D 模型或把二维成功误报为三维成功。
7. 分别模拟转换器未安装、不支持版本、缺失 XREF、单位不明确、转换超时和文件/图元超限，API 与界面显示对应错误和修复动作，且不调用远程 AI、不创建候选或生产 artifact。
8. 恶意嵌入对象、绝对路径/XREF、深层嵌套块和超大渲染不会执行代码、访问网络或逃逸临时目录；达到资源限制时任务可中止、临时文件可清理，其他请求仍可用。

### 2026-09-10 服务器 DWG 兼容性修复与回归

- 生产转换器由 LibreDWG 0.13.3 升级至 0.14，并固定发布源码校验值。`30流道雕铣(1).dwg` 在旧版转换后出现匿名图块引用缺失，完整解析中断；不得通过跳过图块来绕过失败。
- 相同原图在服务器新版完成严格预处理及模型附件准备：398 个原始实体、572 个展开实体、12 个原生尺寸对象、1 个布局、无警告；生产预览接口和浏览器均显示 2036×2880 整页渲染。
- 预处理错误显示具体中文原因和恢复建议，原图与重试依据保留；旧失败记录可在升级后继续重试。该次失败发生在模型调用前，调用次数为 0。
- 本次已发布并验证二维解析、预览与服务健康；三维建模及人工确认仍需后续执行，不将二维解析通过视为交付通过。详情见 [DWG 兼容性排查记录](docs/dwg-compatibility-2026-09-10.md)。

## v0.5.1 远程多阶段读图与失败状态保真（本次迭代）

### 规则

- 图纸轮次先由远程多模态模型提取候选，再由远程模型携带原图审校尺寸链与跨视图语义；支架、开口夹紧座等复杂配方必须再进行一次不携带前轮数值的盲审裁决。对开口夹紧座还要用原图高清局部块分别复读相邻位置尺寸线与高度尺寸链；每个字段两票一致才写入，分歧时增加第三次远程裁决，仍无多数则不写入该字段。中间轮次不更改前端参数，只提交全部远程审校完成后的白名单结果。
- 审校、盲审、位置基准和高度基准专项复核仍使用 `gpt-5.6-sol` / `reasoning=high` / Responses SSE，不设置应用级 `max_output_tokens`；每阶段重新查看原始附件，不使用 OCR、模板值或旧模型参数作为候选或审校输入。局部块只做无标注的图像放大，不产生本地尺寸值。
- 上游连接中断但完整 JSON delta 已到达时可恢复该结果。真正失败时，服务端只回传不含凭证的失败类别和尝试次数；原图保留用于重试。
- 非 `provider.mode=remote` 的上传结果不得创建 evidence、候选卡、确认入口或修改模型。中央与右侧如保留旧实体，必须标记为“上一版本预览”。
- 远程结果未提供字段级置信度时显示“待评估”，不得把本地 OCR 置信度冒充为 AI 置信度。

### 验收

1. 短提示词上传 `9.jpg` 时，SSE 先返回阶段状态，最终候选来自远程审校/裁决轮次，高度链为 `15+40+20=75`、盲孔底面 `Z=40`、圆筒轴心距后缘 `35`、安装孔距后缘 `40`。
2. 上游超时、连接中断、输出未完成、结构化 JSON 异常和空结果可区分；失败后无候选确认按钮，原图仍可一键重试。
3. 无论远程成功或失败，本地 OCR 值均不进入 `candidateParameters`。

## v0.5.0 开口夹紧座参数配方与通用实体生成（本次迭代）

### 目标

把测试图纸 `9.jpg` 从“AI 文字读图结果”接入真正的参数化 CAD 闭环：远程多模态大模型返回零件类型、配方标识、字段候选和证据后，工作台立即切换到开口夹紧座参数面板与同配方三维草稿；客户确认当前数据快照后，再由服务端白名单配方生成 CadQuery/OCCT 实体、STEP 与 GLB。

### 零件类型与尺寸契约

- 零件类型为 `split_clamp_support`，服务端配方为 `split_clamp_support_v1`。`partType` 与 `recipeId` 必须匹配，不得把该候选当作旧 `bracket_support_v1` 或轴类模型处理。
- 坐标约定：X 为底板 125 mm 长度方向，Y 为后端至前端 95 mm 深度方向，Z 为高度方向；底板底面为 Z=0。
- 图纸直接标注、视图投影推导和工艺默认必须分别标识来源。未标注字段可用确定性公式补全，但必须保留 `derived` 来源和公式；预览脚手架值不是图纸证据。

| 结构 | 字段 | 验收值 / 公式 | 读图与拓扑语义 |
| --- | --- | --- | --- |
| 底板包络 | `baseLength` / `baseWidth` / `baseThickness` | 125 / 95 / 15 mm | 俯视外形与主视高度标注 |
| 底板轮廓 | `baseMainDepth` / `frontTongueWidth` | 80 / 80 mm | 125×80 后部主板＋居中 80×15 前舌，总深 95 |
| 底板圆角 | `outerCornerRadius` / `neckConcaveRadius` / `neckConvexRadius` | 8 / 5 / 8 mm | 主板前侧两外角 R8、两处缩颈凹角 R5、前舌两前角 R8；主板后侧两角为方角 |
| 圆筒座外形 | `pedestalOuterRadius` / `pedestalCenterFromRear` | 33 / 35 mm | 下座为前半圆 R33、后半直边延伸至后缘的 D 形；轴心距底板后端 35 |
| 高度堆叠 | `pedestalHeight` / `rearClampRise` / `totalHeight` | 40 / 20 / 75 mm | 15 底板＋40 下座＋20 后半夹环＝总高 75；下座顶面 Z=55 |
| 中央盲孔 | `boreDiameter` / `boreFloorZ` | 36 / 40 mm | 竖直 Ø36 盲孔，孔底 Z=40 |
| 径向开缝 | `splitWidth` | 12 mm | 自中央孔前切点通向外壁，从 Z=40 向上切除 |
| 安装孔 | `mountHoleCount` / `mountHoleDiameter` / `mountHoleCenterDistance` | 2 / 12 / 96 mm | 两处 Z 向贯穿孔，X 中心距 96 |
| 安装孔纵向基准 | `mountHoleCenterFromRear` | 40 mm | 安装孔中心线距后端独立标注 40；不得与圆筒轴心的 35 mm 合并 |
| 横向孔 | `crossHoleDiameter` / `crossHoleCenterZ` | 12 / 55 mm | Y 向贯穿孔；中心在下座顶面 Z=15+40=55，因而形成可见半圆开口，不是 Z=65 |
| 加强筋 / 后桥 | `ribHeight` / `ribThickness` / `rearBridgeWidth` | 20 / 10 / 86 mm | 后高墙为直边矩形后半体；左右对称三角筋位于后缘 10 mm 带，径向延伸由后桥 86 与外径 66 推导 |

### LLM 候选、预览与人工确认

- 图纸轮次的最终结构化结果必须包含顶层 `partType`、`recipeId`、`parameterPatch` 和可选的 `parameterEvidence`。`parameterPatch` 是自动候选的唯一数值源；本地 OCR、旧支架模板和预览默认值不得写入 AI 候选。
- 识别为 `split_clamp_support_v1` 后，工作台必须立即替换右侧参数表、中央参数化 WebGL 草稿、特征树和几何校验规则；不能继续显示上一个轴类/支架模型。
- 参数面板显示模型已返回值、来源视图、原始文字和置信度；确定性派生值显示公式。模型未返回的必填字段保持“待补全”，只允许作为预览的脚手架值不得进入确认载荷。
- 候选持久化状态保持 `pending/needs_review`。客户可在参数面板修改任意候选，被修改字段来源变为 `manual`；点击“确认数据”时，前端同时提交 `partType`、`recipeId` 和 `parameterOverrides`。
- 候选内部同步、切换配方和三维重绘不计入人工编辑修订号。只有客户真实输入产生的修订冲突，才保护现有值不被迟到的 AI 建议覆盖。
- 人工确认必须生成服务端可查的已确认识别 ID；之后以该 ID 作为 `sourceDrawingId` 生成实体。请求参数必须与已确认的配方快照完全一致，否则生成被拒绝。

### 通用几何 API 与生产边界

- 通用校验入口为 `POST /api/v1/models/validate`，通用生成入口为 `POST /api/v1/models/generate`。服务端只调度白名单中的本地配方：未知字段和零件类型/配方不匹配返回 422；几何关系冲突在 validate 报告中返回 `valid=false` 及可审计规则，generate 则返回 422 且不产生 artifact。服务不执行大模型生成的任意 CAD 代码。
- `bracket_support_v1` 与 `split_clamp_support_v1` 使用同一调度契约。旧 `/brackets/validate` 与 `/brackets/generate` 作为兼容入口保留，新工作台应调用通用入口。
- `requireCadQuery=true` 是生产交付门禁：CadQuery/OCCT 不可用时必须明确失败，不得静默降级。faceted/WebGL fallback 可用于确认前草稿预览，但始终为 `productionReady=false`。
- 生产成功必须同时满足：`engine=cadquery-occt`、`validation.valid=true`、`validation.productionReady=true`，生成有效 STEP 和 GLB，并使 STEP 能被 OCCT 重新打开且保持同一实体和包络。
- 客户确认只解锁当前参数实体生成，不等同于 PDM 正式发布、CAM 审批或 NC 制造放行。

### 端到端验收标准

1. 从轴类示例状态上传 `9.jpg` 后，AI 回复与图纸候选同时返回 `partType=split_clamp_support` 和 `recipeId=split_clamp_support_v1`；右侧参数面板、特征树和中央三维草稿立即切换，已返回尺寸与聊天结果一致。
2. 未补全字段清楚显示待确认；客户可直接编辑后提交。内部候选同步不触发“回复期间人工编辑”，真实手动编辑仍能防止迟到建议覆盖。
3. 未确认识别 ID、配方不匹配或当前参数与已确认快照不一致时，生成必须被阻断且不产生交付 artifact。
4. 标准配方的包络为 `125 × 95 × 75 mm`，OCCT 结果为一个有效实体；圆筒轴心距后缘 35、安装孔距后缘 40。D 形下座与矩形后高墙、Ø36 盲孔、2×Ø12 安装孔、孔距 96、Z=55 的 Ø12 横向贯穿孔、12 mm 开缝、后缘筋板、方形后角及前侧 R5/R8 圆角都必须通过真实拓扑审计。
5. STEP 头为有效 ISO-10303-21，GLB 头为 `glTF` 且版本 2；导出链接可下载。STEP 经 CadQuery/OCCT 回读后仍为一个有效实体、包络不变、关键拓扑审计通过。
6. CadQuery/OCCT 不可用时，普通预览可返回显式 fallback；以 `requireCadQuery=true` 请求生产导出时必须失败，且任何 fallback 都不得显示为“生产实体”。

### 限制与非目标

- 本迭代增加第二个经服务端审核的参数配方，并建立可扩展调度边界；不承诺对任意机械图纸生成任意拓扑。新零件类型必须先实现服务端白名单配方、校验和拓扑测试。
- PDF/DWG/DXF 可作为大模型附件，但仍不代表已实现所有原生图元、多页视图或装配约束的自动拓扑重建。
- 草稿预览、AI 文字回复和客户确认均不是制造批准；生产交付仍需 OCCT 实体验证、STEP 回读、PDM 版本和独立 CAM/NC 审批与放行。

## v0.4.1 图纸候选同步预览（本次迭代）

### 规则

- 图纸轮次取得远程大模型的最终 `parameterPatch` 后，工作台必须立即切换到对应零件类型的候选草稿：AI 已返回的字段同步显示在右侧参数面板，中间三维视口同步显示候选几何；不得继续展示上传前的轴类示例模型。
- 未识别字段继续显示“待补全”，用于维持三维草稿的模板值只能作为预览脚手架，不得标记为 AI 识别值或进入确认载荷。
- “回复期间发生编辑”的冲突判断只依据客户真实修改产生的修订号。服务状态、图纸分析状态、旧文件恢复和候选内部同步均不算人工编辑。
- 候选同步只更新可编辑草稿，不改变证据的 `pending` 状态；客户完成补全并点击“确认数据”后，才允许生成 OCCT 实体和导出交付文件。

### 验收标准

1. 从轴类示例状态上传支架图纸，AI 返回底板长宽厚等部分候选后，右侧立即切换为支架参数并显示相同值，中间视口同步出现支架候选预览。
2. 客户没有触碰参数时，不出现“回复期间编辑，未自动覆盖”的提示。
3. 客户确实在 AI 回复期间修改参数时，迟到建议仍只显示在候选卡，不覆盖客户输入。
4. 未补全或未确认的候选无法生成、重建或导出生产实体。

## v0.4.0 可持续交互的 CAD Copilot（本次迭代）

### 目标

把“AI 设计助手”从单次图纸分析表单升级为当前设计工作台内的持续对话。客户可以像使用 Codex 一样连续追问、补充图纸、纠正理解和修改尺寸；AI 必须理解本会话已有消息与当前模型状态，并在等待期间实时显示生成进度，而不是提交后长时间停在一个“处理中”按钮。

### 对话与界面规则

- 同一工作台维护一个明确的对话线程。每轮向远程模型显式传递此前用户/AI 消息和当前可编辑模型状态；即使 `store=false`、中转站不支持 `previous_response_id`，上下文也不能丢失。
- 输入框采用聊天习惯：`Enter` 发送，`Shift+Enter` 换行；支持每轮附加图片、PDF、DWG 或 DXF。发送后立即出现用户消息和空的 AI 回复气泡。
- 浏览器通过 SSE 接收 `status`、`delta`、`result` 与 `error` 事件。AI 自然语言回复应逐段写入同一个气泡；最终 `result` 才携带经过白名单校验的 `parameterPatch`、复核问题和图纸证据。
- 生成期间显示“停止”动作。客户取消后保留已经发送的用户消息和已显示的 AI 文本，但不得应用未完成的参数补丁或生成实体。
- 提供“新对话”动作，清空聊天上下文和上游响应 ID；不得隐式删除已生成 CAD 实体、参数或项目文件。
- 消息区自动跟随最新回复，但客户主动向上阅读历史时不得强制抢回滚动位置。历史消息在当前浏览器工作台恢复，附件只保存文件名等元数据，不把文件内容写入浏览器持久存储。

### CAD 动作规则

- AI 回复与 CAD 参数动作分离：回复可以解释、追问或提出候选；只有最终结构化 `parameterPatch` 才能修改受支持字段。
- 纯文字的明确二次修改可以在参数校验通过后更新当前模型并重建；含新图纸的轮次仍进入“候选数据待确认”，不得绕过人工确认和 OCCT 生产门禁。
- 远程失败时只显示该轮的安全错误类别和“重试”动作；不得把 OCR、模板或上一模型值伪装为本轮模型回答。

### 验收标准

1. 连续发送“当前底板多长？”与“把它改成 120 mm”时，第二轮请求包含第一轮上下文，并返回/应用 `baseLength=120`。
2. 中转站后台和浏览器请求均为流式；页面在最终 JSON 完成前已显示 AI 回复增量或阶段状态。
3. `Enter` 发送、`Shift+Enter` 换行、附件随消息发送、停止生成和新对话均可操作，并具有可访问名称。
4. 取消或流式错误不会应用半个补丁；成功最终事件只应用一次，且保留现有人工确认、几何校验与生产导出门禁。
5. 关闭远程响应存储时仍能完成多轮对话；前端不接触中转站密钥，也不持久化附件二进制数据。

## v0.3.3 远程大模型唯一候选源与流式分析（本次迭代）

### 目标

客户上传图纸后，所有自动写入参数面板的候选值必须来自 `gpt-5.6-sol` 对原始附件的直接多模态分析。本地 OCR、哈希校准配方和几何识别只保留为审计/人工复核证据，不得在远程模型失败或漏字段时替代模型输出。中转站调用使用 Responses SSE 流式协议，避免高推理强度的视觉请求被记录为非流式长等待。

### 交互与数据来源规则

- 第一次请求携带原始图纸的高清视觉输入；可重试错误发生时，第二次仍携带同一原图，以低细节兼容模式进行无历史、无存储重试。不得把 OCR 文本或本地几何候选作为第三次模型输入兜底。
- 上传轮次的 `parameterPatch` 是唯一自动候选参数源。服务端可以返回 OCR 尺寸、视图、置信度和特征作为证据，但不得把这些值合并到 `candidateParameters`、模型配方或参数面板。
- 大模型未返回的必填字段保持空白并标为“待补全”；不得使用支架模板默认值、上一版本参数或验收夹具校准值代填。客户可以人工输入，人工输入后来源标为“人工修改”。
- 大模型返回候选后始终进入同页人工确认；确认提交时继续执行字段白名单、完整性、参数关系和几何约束校验，通过后才允许生成实体。
- 中转站请求体必须设置 `stream=true`，服务端按 Responses SSE 的 `response.output_text.delta` 与 `response.completed` 事件消费结果；兼容中转站聚合为最终 JSON 时仍可解析，但不得把请求退回非流式。
- 应用不得设置 `max_output_tokens`，不得截断对话输入、模型状态或输出文本；模型与中转站自身的上下文能力仍是外部边界。

### 验收标准

1. 中转站后台可看到图纸分析请求为流式；服务状态返回 `streaming=true`。
2. 首次空响应或可重试错误后产生第二次真实网络调用；两次请求都携带相同原图，视觉细节依次为 `high`、`low`。
3. 即使本地 OCR/校准识别出完整尺寸，只要远程模型 patch 为空，API 的 `candidateParameters` 也必须为空，参数面板不得显示这些本地数值。
4. 远程模型返回部分 patch 时，只显示这些 AI 候选；其他必填字段为空并等待人工补全。远程模型返回完整 patch 时，全部字段可编辑确认。
5. 两次远程请求都失败时保留原图和安全失败提示，不生成实体、不创建伪候选，也不宣称整个中转站永久不可用。

## v0.3.2 候选参数默认值回填（历史迭代，已被 v0.3.3 覆盖）

> v0.3.3 已撤销“模板默认自动补齐”规则；本节仅保留版本历史，不再作为当前实现与验收依据。

### 目标

AI 分析完成后，参数面板不得出现整页空白输入框。系统应把图纸/OCR/多模态 AI 已明确提取的数值优先写入；当前支持的零件模板仍有未识别字段时，用可编辑的模板默认候选补齐，让客户通过“确认或修改”完成数据确认，而不是逐项从零录入。

### 交互与安全规则

- 支架候选表的 14 个必填尺寸在 AI 分析结束后必须都有数值；合并优先级为“人工修改 > 图纸/OCR/AI 明确值 > 支架模板默认候选”。
- 每个字段必须展示来源状态：`图纸识别`、`AI 候选`、`模板默认` 或 `人工修改`。模板默认值不得显示为“已识别”，也不得计入图纸证据置信度。
- 分析摘要必须同时给出“已识别/AI 候选字段数”和“模板默认字段数”。模板默认字段仍属于待确认数据，但不再显示空白或“AI 未识别”。
- 用户可以直接修改任意候选值；修改后该字段来源变为“人工修改”。点击“确认数据”即表示确认当前页面显示的完整参数快照，服务端继续执行字段白名单、参数约束与几何规则校验。
- 在显式确认之前，完整默认草案仍保持 `needs_review/pending`，不得自动生成生产实体、显示 OCCT 生产通过或开放生产导出。
- 该规则只补齐当前已选择且受支持的零件模板；不能把上一张图纸的自定义尺寸或验收夹具值冒充成新图纸识别结果。

### 验收标准

1. AI/OCR 返回空参数时，14 个支架必填字段均显示支架模板数值，并逐项标为“模板默认”。
2. AI/OCR 只返回部分参数时，这些值覆盖对应默认值并标为“图纸识别/AI 候选”，其余字段保留模板默认标识。
3. 用户修改一个默认字段后，该字段立即变为“人工修改”；点击“确认数据”会提交当前完整快照，不再要求逐项重新输入。
4. 未点击“确认数据”时，生成 3D 和生产导出继续被阻断；确认与几何校验通过后才进入生成阶段。

## v0.3.1 人工确认闭环（本次迭代）

### 目标

任何图纸都必须先由 AI/视觉模型给出结构化分析，再由人在同一个工作台确认或修改这些候选数据，最后才生成 3D 实体。`needs_review` 只代表“分析结果等待确认”，不是流程终点，也不能让客户被迫跳转到 PDM 才能继续。

### 交互规则

- 上传后先进入 `AI 分析`：返回零件类型、单位、每项尺寸、来源视图、置信度、特征/拓扑假设、未决问题和可编辑候选参数。远端模型不可用时，必须明确显示“本地候选/待确认”，不能伪装成模型结论。
- `确认数据` 与 `生成 3D` 是两个可见步骤。客户可以直接在证据卡或参数面板修改候选值；确认提交时把当前参数快照和原图识别 ID 一起写入确认记录。
- 设计师/客户的确认只解锁当前项目的实体生成；PDM 正式发布、CAM 审批和 NC 放行仍遵循 reviewer/manufacturing 的 RBAC 门禁。
- 已校准图纸也必须在本次会话显示分析结果并经过一次显式确认，不得因为识别状态为 `confirmed` 而跳过客户可见的确认步骤。
- 确认失败、参数校验失败或服务离线时，保留 AI 分析和可编辑候选值，并给出重试/修正动作；不得把失败页面变成“只能登录 reviewer”的死路。

## v0.3.0 客户工作流与工作台体验（本次迭代）

### 目标

让第一次使用的客户只在一个设计工作台内完成完整闭环：

```text
输入 / 上传 → AI 分析 → 确认数据 → 生成 3D → 二次修改 → 导出交付
```

每个阶段只提供一个下一步主操作。上传图纸、AI 分析、候选数据确认、三维实体、参数修改和交付格式必须共享同一个项目上下文，不再要求客户在“3D 建模”和独立“图纸转 3D”页面之间寻找下一步。

### 交互规则

- 首页第一主入口为“上传图纸开始分析”，支持 JPG、PNG、WEBP、PDF、DWG、DXF；选择文件后先进入待处理队列，客户可补充意图，再点击“开始 AI 分析”。
- 3D 工作台顶部固定六阶段进度条，并在当前项目标题旁显示当前阶段与下一步；分析中的任务、待客户确认的任务和已通过 OCCT 的任务使用不同状态文案。
- 分析卡展示零件类型、每一项候选尺寸、来源视图、置信度、AI 问题和拓扑假设。客户确认/修改后才进入实体生成；正式发布仍由 reviewer/PDM 流程负责。
- 新图纸覆盖已有模型时，旧实体只能作为“上一版本预览”保留；低置信或缺失模型值必须为空并标为待补全。不得使用当前模板、旧模型、OCR 或验收夹具值代填并冒充本次大模型结果。
- 生成后的 AI 对话、参数面板、特征树和 WebGL 视口绑定同一模型版本。参数变更会显示“需要重建”，过期 artifact 不得作为当前交付物。
- 导出统一为工作台右上角“导出交付”菜单，集中提供 STEP（生产实体）、GLB（预览）、DXF（工程图）和 JSON（参数/审计）；未完成人工确认或实体校验时不显示可交付状态。
- 高级 PDM、账号、CAM/NC 仍可用，但从侧栏“高级”分组进入，不与客户的设计主路径竞争注意力。

### 响应式验收

- 1280×720 及 1024×768：顶部项目/步骤、AI、视口、参数检查均可见，关键主操作不藏在滚动区域。
- 900px 以下：侧栏收起为图标或隐藏，工作区标签保留为可横向滚动的顶部导航；AI、视口、检查器按顺序纵向排列，上传、生成、导出按钮保持可见。
- 560px 以下：不得出现页面级横向滚动或竖排文字；上传入口必须是带文字的可聚焦按钮，拖放区同时支持键盘触发。

## 1. 产品定位

JoyNiu NewCAD 是面向机械设计师、工艺工程师和制造团队的浏览器 CAD 工作台。它复刻 CurrentCAD 中被验证的核心体验：自然语言生成参数化零件、可视化编辑特征、三维检查、二维工程图、装配规划、标准件库、项目文件管理和多格式交付。

本版本是独立实现，不复制 CurrentCAD 的私有源码、账号数据或受版权保护的内部资源；产品行为以线程中观察到的公开交互为基准。

## 2. 目标用户与价值

| 用户 | 关键任务 | 成功标准 |
| --- | --- | --- |
| 机械设计师 | 用一句话得到可编辑的零件，并快速改尺寸 | 5 分钟内完成一个带通孔和键槽的轴类零件 |
| 工艺工程师 | 检查结构、尺寸和制造提示 | 关键尺寸、特征树和风险提示可追溯 |
| 制造协作团队 | 共享项目文件并导出交付格式 | 项目、版本、导出记录统一管理 |

## 3. 线程证据摘要

- AI 参数化零件：成功生成外径 20、长度 60、通孔 8 的圆柱轴，并可更新到外径 24、长度 70、内径 10。
- 复杂特征：成功追加键槽（宽 6、深 3、长 40），特征树由 6 项增长到 9 项。
- 3D 项目：生成零件可落入云端项目并以只读视图打开，能看到完整特征树。
- 装配 Agent：能识别零件拆分、轴孔关系、同轴约束与间隙问题，但自动实例落地不稳定。
- 2D 编辑器：常用/插入/注释/参数化/批注/工具栏入口明确；2D AI 实际生成曾超时。
- 文件管理：项目、工程图、下载、版本与回收站入口明确；导出提供 DXF/DWG 与版本选择。
- 体验约束：模型校验会增加耗时和积分消耗；积分显示存在延迟或模块独立计数。

## 4. 信息架构

1. **工作台首页**：新建项目、最近项目、AI 建模入口、积分与任务状态。
2. **3D 建模**：AI 对话、参数卡、特征树、三维视口、属性/检查面板。
3. **2D 工程图**：图层、视图、标注、尺寸校验、DXF/DWG 导出。
4. **装配**：零件实例树、同轴/距离/重合配合、间隙风险。
5. **标准件库**：螺钉、螺母、轴承、销钉与快速插入。
6. **项目管理**：项目文件、版本、回收站、导出记录。

## 5. v0.2.0 交付范围（已实现，v0.2.1 继承）

### P0 必须交付

- 桌面式 CAD 工作台布局：深色导航、侧栏、顶部模式切换、中央视口、右侧属性面板、底部状态栏。
- AI 参数化建模：输入自然语言后解析外径/长度/孔径/键槽参数，更新模型与特征树。
- 参数编辑：输入校验、单位显示、恢复默认、实时更新三维预览。
- 特征树：基准、拉伸、孔、倒角、键槽、材质节点，可选中并定位。
- 三维预览：等轴测线框/实体视觉、拖拽旋转、滚轮缩放、视图重置、剖切开关。
- 2D 工程图页：三视图、尺寸标注、图层列表、比例切换、DXF/DWG 模拟导出。
- 装配页：两个示例零件、配合关系卡片、风险提示和“运行检查”。
- 标准件库：分类筛选、搜索、插入到当前模型。
- 项目文件管理：新建、重命名、版本、下载、回收站模拟，数据持久化到浏览器。
- 导出交付：下载 `.step`、`.dxf`、`.json` 参数快照；导出前显示校验结果。

### v0.2.0 已落地的服务能力

- FastAPI 同时提供 `/api/*` 与 `/api/v1/*`，覆盖健康检查、图纸上传、几何校验、
  STEP/GLB 生成、下载和平台业务 router。
- CadQuery/OCCT 使用可选适配器：安装 `[geometry]` 后生成原生 B-Rep STEP；无内核时
  生成确定性 faceted STEP/GLB，强制标记 `productionReady=false`。
- OCR 结果采用“证据优先”结构，包含原图哈希、尺寸/单位、来源视图、证据框、置信度、
  假设、未决项和人工确认状态；可选 Tesseract。未知图纸会先返回 AI 候选数据，
  由客户/设计师确认或修正后才进入几何生成，不会自动确认为正确模型。
- SQLite PDM 保存项目、文件内容、不可变版本、SHA-256、乐观修订号、回收与审计；
  FastAPI 提供版本上传、清单查询和下载。
- 本地账号使用 PBKDF2 密码哈希、HMAC token 和 RBAC，区分 viewer、designer、
  reviewer、manufacturing、admin，并对敏感操作进行权限校验。
- CAM/NC 已有计划、刀具、工序、确定性仿真预检查、碰撞/干涉/包络门禁、审核和 NC
  放行；审批仅限 reviewer/admin，放行仅限 manufacturing/admin，并要求审核者与放行者
  身份分离。预检查不是机床级材料去除仿真，不能单独作为生产批准。

## 6. 核心流程

```text
客户上传图纸 -> 保存原图哈希/版本 -> AI/OCR 提取候选证据 -> 客户确认/修正
       -> 生成参数配方 -> 几何校验 -> CadQuery/OCCT 或 fallback STEP/GLB
       -> PDM 保存参数/模型版本 -> CAM 计划/仿真 -> 审核者批准 -> 制造角色放行 NC

新建项目 -> AI 描述零件 -> 解析参数 -> 人工确认 -> 更新特征树/预览
       -> 运行模型检查 -> 打开 2D 工程图/装配 -> 导出 STEP/DXF/参数快照
```

## 7. 非功能要求

- 首屏可在普通笔记本浏览器流畅打开，交互反馈不超过 200ms（模拟 AI 过程除外）。
- 所有关键状态可恢复：刷新后保留当前项目、参数和最近操作。
- API 输出必须带几何引擎、校验结果和生产就绪标记；fallback 不得冒充 OCCT B-Rep。
- 原图、识别证据、参数、几何生成物和 CAM 决策必须能通过哈希/版本关联审计。
- 不在前端存放真实密钥；服务端通过 `JOYNIU_AUTH_SECRET`、`JOYNIU_DB` 等环境变量配置。

## 8. 验收标准

- 输入“外径24 长度70 通孔10，增加一条宽6深3长40键槽”后，参数卡、预览和特征树同步显示。
- 删除或清空必填参数时，界面给出可理解的错误且不破坏上一次有效模型。
- 2D 页能展示三视图与至少 4 个尺寸标注，并能触发 DXF 下载反馈。
- 装配页展示实例数量、约束数量、风险数量，检查按钮能更新状态。
- 项目页新建项目后可在最近项目中看到，刷新浏览器后仍存在。
- 导出前若模型检查未通过，按钮被禁用；通过后可下载三种文件。

## 9. 版本策略

- v0.1.0：可运行的本地工作台原型（已完成）。
- v0.2.0：FastAPI、CadQuery/OCCT 适配器、图纸证据 OCR、SQLite PDM/RBAC、
  CAM/NC 门禁与验收脚本（已完成并作为几何基线）。
- v0.2.1：多模态 AI Copilot、服务端密钥代理、多轮尺寸编辑和通过验证后的
  STEP/GLB 自动重生成（历史基线）。
- v0.3.0：通用视觉模型与队列、装配实例持久化、对象存储、组织/项目级 ACL、
  STEP 拓扑回读和多人审核流。
- v0.3.1：AI 候选分析与客户确认闭环；未知图纸可在工作台内编辑候选参数并生成实体，
  生产发布/NC 门禁继续由独立角色控制。
- v0.4.0：Responses SSE 连续对话、大模型唯一候选源、候选参数即时同步到工作台。
- v0.5.0：通用白名单配方调度、`split_clamp_support_v1` 开口夹紧座、人工确认交接与
  CadQuery/OCCT STEP/GLB 真实实体验收（当前版本）。
- v1.0.0：云端协作、经过机床验证的 CAM 仿真/后处理、生产级安全与部署。

## 10. v0.2.0 图纸转三维验收增量

### 验收目标

客户上传一张二维机械工程图后，系统必须经过“原图留存、尺寸证据提取、人工确认、确定性几何生成、实体校验、交付文件生成”六个阶段，输出完整且尺寸正确的三维模型。不能只展示相似的示意图，也不能把未验证的文本文件冒充 STEP。

### 本次验收夹具

用户提供的正投影安装支架图纸登记为 `bracket_support_v1`（兼容别名
`acceptance_bracket`），源图 SHA-256 为
`ea337023af0158438f9cea2482e8e2d6d4052fc04e7e7f4265956824478c4366`，关键约束如下：

| 结构 | 验收尺寸 | 来源 |
| --- | --- | --- |
| 底板 | 100 × 50 × 10 mm | 俯视图、主视图 |
| 上部实体 | 70 × 50 × 30 mm | 主视图、右视图（全宽） |
| 总高度 | 40 mm | 主视图 |
| U 型缺口 | 开口 40 mm，R15，槽底 Z=25 mm | 主视图 |
| 矩形顶槽 | 2 ×（宽 X=10、长 Y=30、深=10 mm，槽底 Z=30） | 俯视图、右视图 |
| 竖向贯穿切孔/侧边半圆缺口 | 2 × Ø20 mm，Z=0..40 贯穿 | 俯视图、等轴图 |
| 切孔位置 | X=±35、Y=0，中心距 70 mm | 三视图投影关系 |

> 兼容说明：`bossDiameter`、`bossCenterDistance`、`bossHeight` 旧参数名继续
> 接受，但在本图纸中表示切孔/侧边缺口约束，不表示实体凸台；新配方使用
> `slotLength=30`、`slotWidth=10`、`pocketDepth=10`、`holeDepth=40` 和
> `holeThrough=true` 明确表达几何语义。

### 强制技术能力

- FastAPI 接收图片/PDF/DXF/DWG（栅格化与实体解析按部署能力启用），创建可查询的图纸结果。
- OCR/图纸识别结果必须包含数值、单位、来源视图、置信度和确认状态。
- CadQuery/OCCT 生成真实拓扑实体，并导出 STEP；缺少原生内核时必须明确返回 `fallback`，不能声称生产可用。
- 当前实体生成后核对包络 100 × 50 × 40 mm、参数约束和体积，并在 OCCT 可用时回读
  实体数量、关键圆柱面、圆柱轴线位置和 R15 缺口曲面的拓扑；STEP 会重新导入做一致性
  复核。更完整的通用拓扑规则仍列入 v0.3。
- PDM 保存原图、参数快照、生成物和不可变版本记录。
- RBAC 区分查看者、设计师、审核者、制造放行者和管理员；设计师可生成/仿真，审核者
  可审批，manufacturing 角色才能在独立审批后放行 NC。
- CAM/NC 只生成设计草案；仿真、碰撞检查、后处理器与人工审批未通过时不得标记为生产 NC。

### 端到端通过条件

1. 上传本次图纸后识别出全部 6 组关键约束。
2. 人工确认后生成支架三维预览；安装 CadQuery/OCCT 时同时生成真实 B-Rep STEP，
   未安装时必须返回明确的 fallback 标记。
3. 几何验证报告显示包络、特征和参数一致；生产模式还需 OCCT 拓扑回读。
4. OCCT 模式下 STEP 能被几何内核重新打开，并得到同样的包络尺寸。
5. PDM 中存在原图版本、参数版本和模型版本的关联记录。
6. CAM 接口返回带工序、刀具、余量和安全门禁的 CAM IR；服务中的 `released` 仅表示
   工作流草案已放行，deterministic pre-check 默认不得解释为 `production_released=true`。

## 11. 当前验收记录与复现方式

### 2026-08-29 本地验收结果

使用客户原图（107,532 bytes）运行 `apps/api/scripts/acceptance_check.py`，其 SHA-256
与 fixture 完全一致。在仓库提供的 `apps/api/.venv`（含 FastAPI、CadQuery/OCCT）中，
当前得到 12 项通过、0 项跳过、0 项失败；若仅使用未安装可选依赖的系统 Python，脚本会
保留核心 9 项通过并将 FastAPI/CadQuery 标记为可选跳过：

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 原图与 fixture | 通过 | SHA-256 完全匹配 |
| OCR 尺寸/证据 | 通过 | `confirmed`、16 条尺寸证据、5 个结构特征（含矩形槽与侧边切孔） |
| OCR → 参数配方 | 通过 | 100×50×10、70×50×30；槽长30/宽10/深10；R15 沿 Y 贯穿50；2×Ø20 竖向贯穿切孔、中心距70 |
| RBAC | 通过 | designer 可仿真不可放行；manufacturing 可放行；token 校验通过 |
| PDM | 通过 | 原图与参数各创建 revision 1，不可变内容 SHA-256 匹配 |
| CAM/NC | 通过 | `deterministic-precheck` 通过，reviewer 审批后由独立 manufacturing 角色放行 |
| FastAPI 导入 | 通过（带依赖环境） | `apps/api/.venv` 可导入；无 `apps/api[dev]` 时按可选项跳过 |
| CadQuery/OCCT 导出 | 通过（带依赖环境） | `engine=cadquery-occt`、`productionReady=true`；无 `[geometry]` 时使用显式 fallback |

“跳过”不等于生产验收通过。发布 CI 必须安装开发依赖，并用 `--strict-optional`
把 FastAPI/CadQuery 缺失视为失败；只有 `engine=cadquery-occt` 且
`productionReady=true` 的 STEP 才能进入生产实体复核。

### 复现命令

```bash
# 标准服务/测试环境（建议使用仓库已有虚拟环境）
cd apps/api
python3 -m pip install -e '.[dev,geometry]'
python3 -m pytest
cd ../..

# 精确原图验收
python3 apps/api/scripts/acceptance_check.py --drawing /path/to/drawing.jpg

# 发布门禁：FastAPI/CadQuery 缺失时失败
python3 apps/api/scripts/acceptance_check.py --strict-optional --drawing /path/to/drawing.jpg
```

验收脚本不会写入仓库：PDM/RBAC/CAM 使用临时 SQLite，生成的 token 和 NC 草案只在
验收数据库中存在。真实 API 默认把账号、PDM 文档版本以及 CAM 计划/仿真/审批/NC
快照保存到 `apps/api/data/joyniu.sqlite3`，也可用 `JOYNIU_DB` 改写路径；直接几何
artifact 缓存和 OCR 识别 hand-off map 仍是进程内缓存，但 drawing-to-model 工作流会
把完整识别证据、参数和生成物写入不可变 PDM 版本。

## 12. v0.2.1 多模态 AI Copilot 增量

### 产品目标

用户在 3D 建模工作台中上传工程图或输入自然语言，Copilot 将已有模型状态、对话上下文与附件统一解释为结构化候选分析。候选通过字段白名单和几何规则后更新编辑器；客户/设计师显式确认当前快照后，系统才生成 STEP 和 GLB。

Copilot 是参数编辑和图纸理解助手，不是几何真值源。v0.2.0 的证据确认、CadQuery/OCCT 实体验证、PDM 版本与 CAM/NC 放行门禁继续有效，AI 输出不得绕过它们。

### 功能范围

| 能力 | v0.2.1 行为 | 产品边界 |
| --- | --- | --- |
| 多模态输入 | 对话可同时携带文字和图片、PDF、DXF、DWG；图片会转成保留原审计哈希的 relay-friendly JPEG，默认以 high detail 发送，兼容重试使用 low detail，其余以文件输入转发 | 最多 4 个附件，每个不超过 20 MiB；空文件和不支持类型必须拒绝 |
| 推理模型 | 服务端通过 Responses 兼容端点调用 `gpt-5.6-sol`，默认 `reasoning.effort=high` | 模型、推理强度和端点可由部署环境覆盖；前端只显示非敏感状态 |
| 多轮尺寸编辑 | 提交当前模型 JSON 和 `previousResponseId`，支持“把底板长度改为 110 mm”等连续修改 | 服务仅返回说明、待确认问题和字段白名单内的 `parameterPatch`；未知字段、无效类型和越界数值不能进入编辑器 |
| 图纸证据联动 | 附件同时进入现有 OCR/fixture 证据流用于审计展示；`candidateParameters` 只接收远程多模态模型 patch | 未知图纸保持 `needsReview=true` 直到客户/设计师显式确认；OCR/fixture 数值不得替代、补齐或覆盖模型候选 |
| 自动几何交付 | 客户确认图纸候选后调用几何校验并生成 STEP/GLB；已确认实体的纯文字尺寸修改可继续自动重建 | 上传图纸不得在 AI 分析完成后隐式生成；仅当显式确认、`needsReview=false`、参数有效且几何服务可用时执行；未通过校验时不产生可交付 STEP |
| 服务降级 | 上传图纸无法形成远程候选时保留原图并显示重试；本地路径不得产生图纸候选 | 无附件的明确中文参数修改可保留兼容解析，但不能伪造远程模型成功；图纸候选必须来自多模态模型 |

### 端到端交互

```text
设计师/客户 -> 输入要求 / 上传图纸 -> FastAPI 验证格式和大小
       -> 服务端组合当前模型、对话 ID 和附件 -> gpt-5.6-sol / high
       -> 结构化全量候选分析 -> 应用 allowlisted parameterPatch
       -> 展示参数/来源/置信度/问题 -> 人工确认或修改
       -> 参数/拓扑校验 -> CadQuery/OCCT STEP + GLB
       -> 更新三维视口，保留校验结果、引擎和 productionReady 状态
```

对话回复必须明确区分三种结果：已应用参数、已生成但仅可审阅的预览、通过 OCCT 验证的可交付实体。参数更改后，旧 artifact 立即标记为过期，不能继续作为当前版本下载。

### 服务端密钥与权限

- 供应商凭证仅在 API 进程中通过 `JOYNIU_AI_API_KEY` 或
  `JOYNIU_AI_API_KEY_FILE` 配置；`JOYNIU_LLM_API_KEY` / `JOYNIU_LLM_API_KEY_FILE`
  作为兼容名。密钥不得写入代码、前端 bundle、localStorage、日志或 API 响应。
- `/api/v1/ai/conversation` 默认需要 Bearer token 与 `ai:chat` 权限，仅
  `designer` 和 `admin` 默认具备；匿名演示只能由本地部署显式开启，且仅接受 loopback
  请求或显式 development/local 环境，不影响 PDM/CAM 权限。
- `/api/v1/ai/status` 只暴露模型、推理强度、配置状态与运行模式，不返回密钥。供应商错误向客户端简化为安全消息，不转发原始响应体。
- 图纸会发往配置的远程 AI 端点。生产部署必须先完成数据分类、传输协议、保留周期与供应商合规审查；保密图纸未获授权时不得上传。

### v0.2.1 验收标准

1. 启动 API 并仅在服务端配置密钥后，`/api/v1/ai/status` 显示
   `model=gpt-5.6-sol`、`reasoningEffort=high`、`configured=true`，响应和前端资源中不出现密钥。
2. 未登录调用对话端点返回 401；具有 `ai:chat` 权限的设计师/管理员可调用，无权角色不得代理图纸。
3. 对话框可上传图片、PDF、DXF 和 DWG；服务端正确校验不超过 4 个文件、单件 20 MiB 的限制，且响应中包含文件名、MIME、字节数和 SHA-256 等非敏感附件元数据。
4. 供应商请求使用 `gpt-5.6-sol` 和 `reasoning.effort=high`；图片映射为 `input_image`，PDF/DXF 可映射为 `input_file`。DWG 自 v0.5.2 起不得作为原始 `input_file` 外发，必须先在本地转换、量测，再发送派生渲染图与受限矢量证据。文本会话可携带当前模型状态和有效的上一轮 response ID；附图审校采用显式历史与独立无状态复核。
5. 对验收支架输入“把底板长度改为 110 mm”后，只应用与请求相关的 `baseLength=110` 补丁，参数面板与三维视口同步，上一版 STEP/GLB 标记过期。
6. AI 返回未知字段、错误类型、非有限数字或非正尺寸时，服务端拒绝整个无效补丁，原模型与已有 artifact 不被覆盖。
7. 对精确验收图或已确认支架配方，AI 分析后先展示候选参数；客户/设计师点击“确认数据”后才调用几何生成。在 CadQuery/OCCT 验收环境中同时得到有效 STEP 和 GLB，校验报告 `valid=true`，STEP 为 `productionReady=true`。
8. 当 `needsReview=true` 或图纸未确认时，界面必须显示完整候选数据、来源/置信度、AI 问题和“确认数据”动作；不得把页面停在只有 reviewer 登录入口的终点。参数规则失败、API 离线或 OCCT 不可用时，仍需显示可修正/重试状态。
9. 客户/设计师确认后，服务端必须记录 `confirmationType`、`confirmedBy`、`confirmedAt` 和参数快照；只有确认后的识别 ID 才能进入几何生成。远端 AI 断网时可以保留本地候选，但不能把候选或 fallback 冒充为生产实体。

### 已知限制与非目标

- v0.2.1 的 AI 补丁覆盖当前白名单中的轴类/支架参数，不是任意机械零件、装配体或自由曲面的通用 CAD 程序合成器。自动 STEP/GLB 重生成当前以已支持的参数化支架配方为验收范围。
- PDF/DXF 可在 Copilot 中作为文件证据交给远程模型；DWG 自 v0.5.2 起先走本地原生转换、尺寸实体读取、矢量量测与高清渲染。当前仍不保证所有自定义/proxy DWG 实体、全部 DXF 图元拓扑或多页 PDF 视图都能自动对齐到唯一三维特征树。
- 多模态模型可能误读投影关系、隐线、公差、粗糙度、焊接符号和标题栏单位。没有来源证据或有冲突的尺寸必须询问用户，不允许自动补全。
- 供应商延迟、限流、可用性和 Responses 保留策略受外部服务影响。部署方可关闭远端 response 存储，但必须同时接受多轮上下文能力受限。
- AI 回复、GLB 预览和 faceted STEP 均不是制造批准。只有通过 OCCT B-Rep 校验、STEP 回读、PDM 版本和独立 CAM 审批/放行的产物才能继续生产流程。


## 2026-09-13 前端信息层级调整

- 全站使用单一侧栏：创模 AI、项目、任务、交付、教程为常用入口，专业工具与账号功能按需展开。
- 首页以设计描述和图纸附件作为主要入口，最近项目按更新时间排序；导航返回保留未发送描述，附件与描述一起带入建模。
- 建模页以对话与模型为主体；参数、特征树和检查按需打开，流程与历史折叠。待补充信息、失败原因、草稿状态和交付条件持续可见。
- 手机端使用对话／模型／参数与检查三个视图；回答问题会切换到可见输入框，跨页编辑参数会显式展开面板，模型放大使用全部页面空间。
- 二维编辑按选择、绘制、标注、变换分组，只显示当前工具设置；工程与特征编辑收起来源、历史、剖视和表达式等次要设置。
- 保留原有保存、上传校验、真实实体生成、确认及导出流程。验收和前端发布证据见 [界面简化验收](docs/frontend-simplification-2026-09-13.md)。

## 2026-09-13 生成后继续编辑三维模型

> 此节记录此前已发布版本的范围；用户已明确要求按 CurrentCAD 的编辑方法和前端继续重建。新的验收以文末修订为准，不能把本节的旧限制视为用户接受的交付边界。

- 生成结果页提供“编辑模型”，完整带入当前 CAD 特征计划、尺寸表达式及来源版本。保留参数配方模型原来的参数编辑入口；没有特征历史的网格不自动伪造特征树。
- 编辑器按特征树、实体视口、所选特征属性布局；提供草图拉伸、拉伸切除、旋转添加、圆角、倒角、阵列、抽壳及已有建模操作。添加/切除明确引用当前输出实体。
- 拉伸/旋转草图支持控制点选择、拖动、方向键、精确坐标、直线、三点圆弧和矩形/圆轮廓。表达式坐标须显式替换，保留未知尺寸。
- 保存草稿和重建实体均关联来源项目文件；返回、刷新及重新登录可继续编辑保存版本。原 AI 计划与确认状态独立保留。手工 STEP/GLB 下载绑定精确版本，未重建草稿不下载旧几何。
- 本次不包括任意 STEP/网格的特征逆向恢复、三维视口逐面/逐边直接修改、完整约束求解草图、自由曲面及 CurrentCAD 全部 PMI 工具。与原图的一致性仍需重新核对。
- 验收依据见 [模型编辑验收](docs/model-editing-2026-09-13.md)。

## 2026-09-13 修订：CurrentCAD 编辑方法与前端对齐

用户要求是生成后进入完整 CAD 编辑环境，操作方法与参考界面一致。单纯展示参数表、增加编辑按钮或换配色不能满足此要求。多人实时协作、小程序及密码受限分享仍按之前约定暂不纳入。

- 独立零件编辑界面取代业务侧栏布局：顶部品牌/图档/退出编辑，紧凑工具栏，左侧默认几何元与特征/草图树，中央实体画布，底部真实图档标签。常态不显示大面积参数表。
- 双击特征打开画布左上角的小型操作窗；双击草图进入正交全画布草图编辑。草图工具与“完成草图”在同一工具栏，未选对象不占用属性侧栏。
- 鼠标拾取必须命中真实 OCCT 面或边曲线，所选几何高亮；选平面建立带局部坐标系的草图，圆角/倒角/抽壳操作引用实际选中的几何对象。
- 参数表达式直接驱动草图显示；图上尺寸可以编辑，拖动与尺寸变化保留驱动关系。几何约束须真正参与求解与校验，冲突不能静默忽略。
- 每次操作有独立预览、完成与取消。预览不创建保存版本；完成仅在真实几何构建、实体检查、STEP 回读成功后原子发布一个新版本。失败不覆盖已保存模型；同一请求重试不重复创建版本。
- 文件、账号、输入实体版本和拓扑引用必须一致；旧文件迟到的预览或完成响应不得作用于新文件。旋转、平移和缩放在同一图档的特征预览更新中保持。
- 保存后能够从来源项目继续编辑同一特征历史，按精确保存版本下载 STEP/GLB。原 AI 计划、原图核验和人工确认状态仍独立保留。
- 逐项检验参考界面的实际工具行为、空文档到实体、多图档切换以及完整编辑链路；尚未实现或尚未实测的参考操作必须记录，不能仅以工具栏图标宣布一致。

实施与逐项证据持续记录于 [CurrentCAD 编辑工作区对齐验收](docs/currentcad-editor-parity-2026-09-13.md)。此修订尚在实施，生产上线与完整对齐须单独验收。


## 2026-09-13 修订：完整编辑闭环与交付验收

用户明确拒绝以上一轮未实现能力清单作为交付终点。此轮继续实现多轮廓/孔洞草图、椭圆/样条和几何约束、任意截面扫掠/放样、独立草图、构造几何、曲面、关联 PMI、真实标准件、实体命令、导入直接编辑、PDM 与资源社区。此前章节描述历史版本；以下要求为本轮准入标准。

1. 草图通过鼠标绘制、拖动和图上尺寸驱动；同一草图容纳多个外轮廓和关联孔洞，支持安全表达式和约束冲突反馈。独立草图可保存、双击编辑、复用于拉伸/旋转/扫掠/放样；删除有引用的草图须阻止。
2. 任意轮廓扫掠沿真实线、弧、样条或构造螺旋线生成实体；放样保留各截面的独立坐标系。不能仅替换成固定圆管或矩形外包络。
3. 平面支持偏移、角度、三点与显式坐标系；轴和点支持精确位置。特征使用构造引用并随参数重建；移除正在使用的参考几何须阻止。
4. 曲面偏移、边界混合、样式控制网格、连接、加厚与移动面实际调用内核。控制网格在三维视口可拖动；G0/G1/G2 边界以实体几何核验。
5. PMI 支持距离、角度、半径、直径、注释、基准、形位公差和粗糙度。鼠标拾取真实面/边位置，使用已授权拓扑和参数锚点随模型变化；无法唯一关联时不能按旧索引猜测。标注保存重开并可导出含当前模型和引线的 SVG 图。
6. 实体可独立选择、命名、隐藏、改色、移动、旋转、复制、保留与删除。显示状态随可唯一识别的实体重建；显式删除实体同步移除其显示记录，不将旧记录套到其他实体。
7. 标准件从目录生成真实几何并原位插入，参数与位置保留为特征。导入 STEP/STP 或闭合 STL 后可在实际几何上选择面/边继续建模；导入资产由账号授权并校验内容，不伪造原始建模历史。
8. 保存到 PDM 包含精确已提交版本、可编辑计划、实体文件及导入依赖；打开为独立副本，修改副本不改变原版本。账号、项目权限、幂等和文件完整性由服务器校验。
9. 资源社区由用户主动发布当前精确已保存模型；发布前说明公开的内容。浏览、搜索、预览、下载、打开独立副本和作者撤下必须真实可用；私有来源标识及附件不得随公共资源泄露。
10. 维持 CurrentCAD 参考的紧凑工具栏、左特征树、主三维画布与底部图档。新增功能使用对应操作窗，避免将全部表单长期堆在页面。桌面及 390 px 手机端均检验可点击、取消、错误反馈和页面宽度。

发布必须基于新源码和构建快照，以真实浏览器交互、OCCT/STEP 回读、保存重开、权限/失败恢复以及隔离 Linux 候选验收为依据；发布记录明确实际版本和证据。多人实时协作、小程序和密码受限分享仍排除。竞品成功率 100% 仅作用户指定的规划假设。

### 2026-09-13 客户操作路径优化

二维和三维全屏编辑器须提供明确且在手机可见的返回主页入口，离开前保护未完成输入。需要登录的工具须提供就地登录，并在成功后回到发起登录的工具。二维文件操作成功后收起菜单，新文档回到模型空间；窄屏须可见图纸名及保存状态。任务、交付空状态须提供下一步入口，长账号标识不得撑出页面。照片附加成功提示必须依据实际附加结果。验收范围与结果见 `docs/customer-ux-2026-09-13.md`。
